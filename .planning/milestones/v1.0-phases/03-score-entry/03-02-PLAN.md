---
phase: 03-score-entry
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/utils/sortComparators.ts
  - src/lib/utils/sortComparators.test.ts
  - src/lib/components/ScoreTable.svelte
  - src/lib/views/ScoreEntry.svelte
  - src/lib/i18n/strings.de.ts
  - src/lib/views/ScoreEntry.test.ts
autonomous: true
requirements: [SCORE-04]
user_setup: []

must_haves:
  truths:
    - "Trainer can click the Linie, Name, Klasse, or Summe column headers to sort the score table; a second click on the same header reverses the direction."
    - "Sort order is ephemeral UI state — reloading the app resets the table to the default (by Linie, ascending), it is never persisted to IndexedDB."
  artifacts:
    - path: "src/lib/utils/sortComparators.ts"
      provides: "sortRows + 4 comparator functions, SortColumn/SortDirection types"
      exports: ["sortRows", "compareByLine", "compareByName", "compareByClass", "compareBySum", "SortColumn", "SortDirection"]
  key_links:
    - from: "src/lib/views/ScoreEntry.svelte"
      to: "src/lib/utils/sortComparators.ts"
      via: "sortRows(built, sortBy, sortDir) replaces the Plan 03-01 inline by-line sort"
      pattern: "sortRows\\("
    - from: "src/lib/components/ScoreTable.svelte"
      to: "src/lib/views/ScoreEntry.svelte"
      via: "onsort(column) callback fired by clickable column headers"
      pattern: "onsort\\("
---

## Phase Goal

**As a** trainer, **I want to** enter and save per-arrow scores live, without losing data, **so that** I can run the live tournament with confidence, even mid-entry disruptions.

<objective>
Add SCORE-04's clickable column-header sorting (Linie/Name/Klasse/Summe) on top of Plan 03-01's score table, using pure/tested sort comparators and ephemeral (non-persisted) sort state.

Purpose: Lets the trainer reorder the live score table for readability (e.g. by line during range calls, or by Summe to spot-check leaders) without touching tournament data.
Output: Sortable ScoreTable with ▲/▼ indicators and `aria-sort`, backed by a fully unit-tested sortComparators module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-score-entry/03-CONTEXT.md
@.planning/phases/03-score-entry/03-UI-SPEC.md
@.planning/phases/03-score-entry/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Consumed from Plan 03-01 — no re-derivation needed. -->

From src/lib/components/ScoreTable.svelte (Plan 03-01, `<script module>` export):
```
export interface ScoreRow {
  shooterId: number;
  name: string;
  className: string;
  line: number | null;
  arrows: (ScoreValue | null)[];
  sum: number | null;
}
```

From src/lib/views/ScoreEntry.svelte (Plan 03-01):
- `rows: ScoreRow[]` built via `$derived.by` from `shooters`/`classNameById`/`currentPasseScoreByKey`/`roundsConfig`, currently sorted inline by `line` ascending — this plan's Task 2 replaces that inline sort call with `sortRows(...)`.
- `<ScoreTable {rows} arrowsPerPasse={...} finalized={isFinalized} oncelltap={openPicker} />` — this plan's Task 2 adds `sortBy`/`sortDir`/`onsort` props to this call site.

<!-- Contract this plan defines. Plan 03-03 does not depend on it, but re-reads
     ScoreEntry.svelte's post-Plan-03-02 state via read_first. -->
From src/lib/utils/sortComparators.ts (this plan, Task 1):
```
export type SortColumn = 'line' | 'name' | 'class' | 'sum';
export type SortDirection = 'asc' | 'desc';
export function sortRows(rows: ScoreRow[], column: SortColumn, direction: SortDirection): ScoreRow[];
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sort comparator functions</name>
  <files>src/lib/utils/sortComparators.ts, src/lib/utils/sortComparators.test.ts</files>
  <read_first>src/lib/components/ScoreTable.svelte (Plan 03-01's exported ScoreRow type), src/lib/utils/modeDetection.test.ts (pure-function test style to mirror), .planning/phases/03-score-entry/03-CONTEXT.md (D-04), .planning/phases/03-score-entry/03-UI-SPEC.md ("Column Headers (Sortable)" section)</read_first>
  <behavior>
    - compareByLine sorts a row with `line: 1` before a row with `line: 2`, and a row with `line: null` sorts after both
    - compareByName sorts "Anna" before "Bob" via `localeCompare`
    - compareByClass sorts alphabetically by `className`
    - compareBySum sorts a row with `sum: 5` before a row with `sum: 10`, and a row with `sum: null` (incomplete) sorts before (lower than) any filled sum
    - sortRows(rows, 'line', 'desc') reverses the ascending-by-line order
    - sortRows(rows, 'sum', 'asc') places a row with `sum: null` before a row with `sum: 5`
  </behavior>
  <action>
    Create sortComparators.ts importing `type { ScoreRow } from '../components/ScoreTable.svelte'` (matches the codebase's established pattern of importing named exports/types directly from a `.svelte` file's `<script module>` block, as ShooterForm.svelte already does with `AutoAssignModal.svelte`'s `RosterEntry`). Export `type SortColumn = 'line' | 'name' | 'class' | 'sum'` and `type SortDirection = 'asc' | 'desc'`. Export `compareByLine(a: ScoreRow, b: ScoreRow): number` as `(a.line ?? Number.MAX_SAFE_INTEGER) - (b.line ?? Number.MAX_SAFE_INTEGER)`, `compareByName(a, b)` as `a.name.localeCompare(b.name)`, `compareByClass(a, b)` as `a.className.localeCompare(b.className)`, `compareBySum(a, b)` as `(a.sum ?? -1) - (b.sum ?? -1)`. Export `sortRows(rows: ScoreRow[], column: SortColumn, direction: SortDirection): ScoreRow[]`: look up the matching comparator from a `{ line: compareByLine, name: compareByName, class: compareByClass, sum: compareBySum }` object, return `[...rows].sort(comparator)`, reversed via `.reverse()` when `direction === 'desc'`. Create sortComparators.test.ts mirroring modeDetection.test.ts's plain `describe`/`it` style, covering every <behavior> case above with literal `ScoreRow` fixture objects.
  </action>
  <acceptance_criteria>
    - `npm run test -- src/lib/utils/sortComparators.test.ts` passes all 6 behavior cases listed above.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/utils/sortComparators.test.ts</automated>
  </verify>
  <done>sortComparators.ts exports SortColumn/SortDirection/sortRows and all four comparators; fully unit-tested including null/tie edge cases for line and sum.</done>
</task>

<task type="auto">
  <name>Task 2: Wire clickable column-header sorting into the score table</name>
  <files>src/lib/components/ScoreTable.svelte, src/lib/views/ScoreEntry.svelte, src/lib/i18n/strings.de.ts, src/lib/views/ScoreEntry.test.ts</files>
  <read_first>src/lib/utils/sortComparators.ts (Task 1), src/lib/components/ScoreTable.svelte (current, from Plan 03-01), src/lib/views/ScoreEntry.svelte (current, from Plan 03-01), .planning/phases/03-score-entry/03-UI-SPEC.md ("Column Headers (Sortable)" and "Accessibility Requirements" sections)</read_first>
  <action>
    Add to strings.de.ts's `scoring` section: `sortAscending: 'aufsteigend sortiert'`, `sortDescending: 'absteigend sortiert'`. Extend ScoreTable.svelte's props with `sortBy: SortColumn`, `sortDir: SortDirection`, `onsort: (column: SortColumn) => void` (import `type { SortColumn, SortDirection } from '../utils/sortComparators'`). Turn the Linie/Name/Klasse/Summe `<th>` contents into `<button type="button" onclick={() => onsort('line')}>` (and `'name'`/`'class'`/`'sum'` respectively) showing the column label plus a `▲`/`▼` suffix only when `sortBy` matches that column (`▲` for `sortDir === 'asc'`, `▼` for `'desc'`); set `aria-sort` on each of those `<th>` elements (`'ascending'`/`'descending'` when `sortBy` matches and that's the direction, else `'none'`). The arrow-number column headers (1..N) and their `<th>` elements remain plain text with NO `onsort`/`onclick` handler — SCORE-04's sortable set is explicitly Linie/Name/Klasse/Summe only. In ScoreEntry.svelte: add `let sortBy = $state<SortColumn>('line'); let sortDir = $state<SortDirection>('asc');` (import `type { SortColumn, SortDirection }` and `{ sortRows }` from `../utils/sortComparators`); add `function handleSort(column: SortColumn) { if (sortBy === column) { sortDir = sortDir === 'asc' ? 'desc' : 'asc'; } else { sortBy = column; sortDir = 'asc'; } }`; replace the Plan 03-01 inline `(a, b) => (a.line ?? Number.MAX_SAFE_INTEGER) - (b.line ?? Number.MAX_SAFE_INTEGER)` sort call inside the `rows` `$derived.by` block with `sortRows(built, sortBy, sortDir)`; pass `sortBy`, `sortDir`, and `onsort={handleSort}` into the existing `<ScoreTable>` call site alongside the props already wired in Plan 03-01. Extend ScoreEntry.test.ts with a sort-interaction test.
  </action>
  <acceptance_criteria>
    - `npm run test -- src/lib/views/ScoreEntry.test.ts` passes including: clicking the "Name" header sorts rows alphabetically ascending; clicking it again reverses to descending; clicking "Linie" afterward switches the active sort column back to line/ascending.
    - `grep -c "aria-sort" src/lib/components/ScoreTable.svelte` returns a value >= 1.
    - The arrow-number `<th>` loop in ScoreTable.svelte contains no `onclick=` handler (grep `src/lib/components/ScoreTable.svelte` for the arrow-header block and confirm no `onsort`/`onclick` reference there).
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/views/ScoreEntry.test.ts</automated>
  </verify>
  <done>SCORE-04 satisfied: Linie/Name/Klasse/Summe headers are clickable, toggle ascending/descending with a visible ▲/▼ indicator and `aria-sort`, and sort state remains ephemeral `$state` (not written to Dexie).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Column-header click → in-memory sort of already-validated ScoreRow[] | No external/untrusted input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-04 | Tampering | sortComparators.ts inputs | accept | Operates only on already-typed, already-validated in-memory `ScoreRow[]` derived from Dexie records; no raw user-supplied string parsing beyond `localeCompare` on names that are already safely rendered elsewhere |
| T-03-05 | Denial of Service | Re-sorting a large shooter roster | accept | `.sort()` on an in-memory array bounded by realistic tournament roster sizes (well under 200); recomputed only on header click or underlying data change, not per keystroke |
</threat_model>

<verification>
- `npm run test -- src/lib/utils/sortComparators.test.ts src/lib/views/ScoreEntry.test.ts`
- Manual smoke: `npm run dev`, navigate to Erfassung, click each column header twice, confirm ▲/▼ toggles and row order changes correctly.
</verification>

<success_criteria>
- All Task 1 and Task 2 automated tests pass.
- All four sortable columns (Linie/Name/Klasse/Summe) toggle ascending/descending on repeated clicks.
- Sort state is never written to IndexedDB.
</success_criteria>

<output>
Create `.planning/phases/03-score-entry/03-02-SUMMARY.md` when done
</output>
