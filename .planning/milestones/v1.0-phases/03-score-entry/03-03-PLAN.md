---
phase: 03-score-entry
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/lib/utils/scoreCompletion.ts
  - src/lib/utils/scoreCompletion.test.ts
  - src/lib/views/ScoreEntry.svelte
  - src/lib/views/ScoreEntry.test.ts
  - src/lib/i18n/strings.de.ts
  - e2e/scoring.spec.ts
autonomous: true
requirements: [SCORE-06, SCORE-07]
user_setup: []

must_haves:
  truths:
    - "Abschließen is disabled until every registered shooter, every configured round, every passe, and every arrow has a value (D-09)."
    - "Clicking Abschließen shows a non-dismissible confirmation naming the permanent, irreversible nature of the action (D-10)."
    - "Once confirmed, every score cell across all rounds/passes and the Runde/Passe dropdowns become permanently disabled, with a visible 'locked' message — no unlock control exists anywhere in the app (D-10)."
  artifacts:
    - path: "src/lib/utils/scoreCompletion.ts"
      provides: "areAllScoresEntered pure function (all shooters x all rounds x all passes x all arrows)"
      exports: ["areAllScoresEntered"]
    - path: "e2e/scoring.spec.ts"
      provides: "E2E coverage that the finalize lock survives a page reload (proves it's persisted in IndexedDB, not just component state)"
  key_links:
    - from: "src/lib/views/ScoreEntry.svelte"
      to: "src/lib/utils/scoreCompletion.ts"
      via: "isComplete = areAllScoresEntered(...) gates the Abschließen button"
      pattern: "areAllScoresEntered\\("
    - from: "src/lib/views/ScoreEntry.svelte"
      to: "db.scores"
      via: "db.scores.bulkPut(...) sets finalized: true on every record on confirm"
      pattern: "db\\.scores\\.bulkPut\\("
    - from: "src/lib/views/ScoreEntry.svelte"
      to: "src/lib/components/ConfirmDialog.svelte"
      via: "non-dismissible finalize confirmation, reusing the Phase 2 component"
      pattern: "ConfirmDialog"
---

## Phase Goal

**As a** trainer, **I want to** enter and save per-arrow scores live, without losing data, **so that** I can run the live tournament with confidence, even mid-entry disruptions.

<objective>
Add completion detection and the permanent "Abschließen" finalize/lock action on top of Plans 03-01/03-02's score table, completing the phase's requirement set.

Purpose: SCORE-06/07 are the tournament-closing half of the phase's core value — the trainer must be able to tell when scoring is truly done and lock it down with confidence that nothing can be silently changed afterward.
Output: A gated, confirmed, permanent finalize action; all score-entry requirements (SCORE-01 through SCORE-07) complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-score-entry/03-CONTEXT.md
@.planning/phases/03-score-entry/03-UI-SPEC.md
@.planning/phases/03-score-entry/03-01-SUMMARY.md
@.planning/phases/03-score-entry/03-02-SUMMARY.md
</context>

<interfaces>
<!-- Consumed from Plans 03-01/03-02 — no re-derivation needed. -->

From src/lib/components/ConfirmDialog.svelte (Phase 2, unchanged):
```
let { open, title, body, confirmLabel, cancelLabel, destructive = false, onconfirm, oncancel }: {
  open: boolean; title: string; body: string; confirmLabel: string; cancelLabel: string;
  destructive?: boolean; onconfirm: () => void; oncancel: () => void;
} = $props();
```
Wiring pattern to follow exactly: src/lib/views/PresetList.svelte's existing `<ConfirmDialog ... onconfirm={...} oncancel={...} />` call sites.

From src/lib/views/ScoreEntry.svelte (Plans 03-01/03-02, current state this plan extends):
- `roundsConfig`, `allScores`, `shooters`, `isFinalized` (already derived in Plan 03-01), `sortBy`/`sortDir` (Plan 03-02) all already exist — this plan adds `isComplete`, `finalizeDialogOpen`, and the finalize handlers alongside them.
- `<RoundPasseSelector ... disabled={isFinalized} .../>` and `<ScoreTable ... finalized={isFinalized} .../>` are already wired from Plan 03-01 — no change needed to those bindings, only verify they still reference `isFinalized`.

From src/lib/utils/scoreCompletion.ts (Plan 03-01's existing exports, extended by this plan's Task 1):
```
export function arrowScoreValue(value: ScoreValue): number;
export function calculatePasseSum(values: ScoreValue[]): number;
export function areAllScoresEntered(shooterIds: number[], numberOfRounds: number, passesPerRound: number, arrowsPerPasse: number, scores: ScoreRecord[]): boolean;  // added by this plan
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Completion detection (all-cells-filled)</name>
  <files>src/lib/utils/scoreCompletion.ts, src/lib/utils/scoreCompletion.test.ts</files>
  <read_first>src/lib/utils/scoreCompletion.ts (Plan 03-01's arrowScoreValue/calculatePasseSum), src/lib/db/schema.ts (ScoreRecord/RoundConfig/ShooterRecord shapes), .planning/phases/03-score-entry/03-CONTEXT.md (D-09), .planning/phases/03-score-entry/03-RESEARCH.md ("Pitfall 2: Completion Check Doesn't Account for All Shooters")</read_first>
  <behavior>
    - areAllScoresEntered([], 1, 10, 3, []) === true (no shooters registered — vacuously complete per UI-SPEC "Completion Check Edge Cases")
    - areAllScoresEntered([1], 1, 1, 3, [3 records for shooterId 1, roundIndex 0, passeIndex 0, arrowIndex 0/1/2]) === true
    - areAllScoresEntered([1], 1, 1, 3, [only 2 of those 3 records]) === false
    - areAllScoresEntered([1], 2, 1, 3, [all of roundIndex 0's records present, roundIndex 1's absent]) === false (must cover every configured round, not just the first)
    - areAllScoresEntered([1, 2], 1, 1, 1, [only shooterId 1's record present]) === false (must cover every shooter, not just some)
  </behavior>
  <action>
    Extend scoreCompletion.ts with `export function areAllScoresEntered(shooterIds: number[], numberOfRounds: number, passesPerRound: number, arrowsPerPasse: number, scores: ScoreRecord[]): boolean` (import `type { ScoreRecord }` from `../db/schema`). Implementation: build a `Set<string>` of existing cell keys via `new Set(scores.map(s => \`${s.shooterId}-${s.roundIndex}-${s.passeIndex}-${s.arrowIndex}\`))`; loop `shooterId` over `shooterIds`, `roundIndex` from `0` to `numberOfRounds - 1`, `passeIndex` from `0` to `passesPerRound - 1`, `arrowIndex` from `0` to `arrowsPerPasse - 1`; return `false` the first time a constructed key is missing from the set; return `true` once every combination is confirmed present (the `shooterIds.length === 0` case returns `true` because the loop body never executes). Extend scoreCompletion.test.ts with the <behavior> cases above, following the existing `describe('calculatePasseSum', ...)` block's fixture style.
  </action>
  <acceptance_criteria>
    - `npm run test -- src/lib/utils/scoreCompletion.test.ts` passes all 5 behavior cases including the empty-shooters vacuous-true case and the "missing one shooter"/"missing one round" false cases from RESEARCH.md Pitfall 2.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/utils/scoreCompletion.test.ts</automated>
  </verify>
  <done>areAllScoresEntered is pure, tested, and correctly requires every shooter x every round x every passe x every arrow per D-09 — not just the currently-viewed round/passe.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize action and permanent lock</name>
  <files>src/lib/views/ScoreEntry.svelte, src/lib/views/ScoreEntry.test.ts, src/lib/i18n/strings.de.ts, e2e/scoring.spec.ts</files>
  <read_first>src/lib/utils/scoreCompletion.ts (Task 1's areAllScoresEntered), src/lib/components/ConfirmDialog.svelte, src/lib/views/PresetList.svelte (ConfirmDialog wiring pattern), src/lib/views/ScoreEntry.svelte (current, from Plans 03-01/03-02), .planning/phases/03-score-entry/03-UI-SPEC.md ("Finalization UI" section), e2e/nav.spec.ts (Playwright test structure/conventions for this repo), playwright.config.ts (webServer production-build convention)</read_first>
  <action>
    Add to strings.de.ts's `scoring` section: `completionHelper: 'Erfassung ist noch nicht vollständig.'`, `finalizeButton: 'Turnier abschließen'`, `finalizeModalTitle: 'Turnier abschließen?'`, `finalizeModalBody: 'Diese Aktion sperrt alle Ergebnisse und kann nicht rückgängig gemacht werden. Fortfahren?'`, `finalizeConfirmYes: 'Ja, abschließen'`, `finalizeConfirmCancel: 'Abbrechen'`, `finalizedMessage: 'Erfassung abgeschlossen. Die Ergebnisse sind jetzt gesperrt.'`.
    In ScoreEntry.svelte: import `ConfirmDialog` from `../components/ConfirmDialog.svelte` and `{ areAllScoresEntered }` from `../utils/scoreCompletion`. Add `let finalizeDialogOpen = $state(false);`. Add `let isComplete = $derived(roundsConfig ? areAllScoresEntered(shooters.map(s => s.id!), roundsConfig.numberOfRounds, roundsConfig.passesPerRound, roundsConfig.arrowsPerPasse, allScores) : false);` — distinct from the existing `isFinalized` (Plan 03-01): `isComplete` gates whether Abschließen is enabled, `isFinalized` reflects whether it has already been clicked and confirmed. Add `async function handleFinalizeClick() { finalizeDialogOpen = true; }`, `async function handleFinalizeConfirm() { errorFeedback = ''; try { const all = await db.scores.toArray(); await db.scores.bulkPut(all.map(s => ({ ...s, finalized: true }))); } catch (err) { errorFeedback = strings.common.saveError.replace('{error}', err instanceof Error ? err.message : String(err)); } finalizeDialogOpen = false; }`, `function handleFinalizeCancel() { finalizeDialogOpen = false; }`. In the template, when `roundsConfig` exists and `!isFinalized`: below `<ScoreTable>` add a button `disabled={!isComplete}` with `onclick={handleFinalizeClick}` labeled `strings.scoring.finalizeButton`, and — only when `!isComplete` — a `<p role="status" aria-live="polite">{strings.scoring.completionHelper}</p>` helper (per UI-SPEC Accessibility Requirements). When `isFinalized` is true, hide the finalize button entirely and render `<p>{strings.scoring.finalizedMessage}</p>` instead. Add `<ConfirmDialog open={finalizeDialogOpen} title={strings.scoring.finalizeModalTitle} body={strings.scoring.finalizeModalBody} confirmLabel={strings.scoring.finalizeConfirmYes} cancelLabel={strings.scoring.finalizeConfirmCancel} destructive={true} onconfirm={handleFinalizeConfirm} oncancel={handleFinalizeCancel} />` (matches PresetList.svelte's existing ConfirmDialog wiring exactly). Confirm `RoundPasseSelector`'s `disabled` prop and `ScoreTable`'s `finalized` prop are still bound to `isFinalized` (already wired in Plan 03-01 — no change needed if so).
    Create e2e/scoring.spec.ts (mirrors e2e/nav.spec.ts's `test.describe`/`test` structure, runs against the `vite preview` production build per playwright.config.ts's existing `webServer`): a test that drives the UI end-to-end — navigate to Einrichtung, add a class, set 1 shooting line and a custom round config of 1 round / 1 passe / 1 arrow, navigate to Schützen, register 1 shooter, navigate to Erfassung — taps the single arrow cell to a value, clicks "Turnier abschließen", confirms via "Ja, abschließen", and asserts the arrow-cell button is now `disabled`; a second test that reloads the page after finalizing and asserts the button remains `disabled` (proving the lock persists via IndexedDB across reload, not just in-memory component state, per SCORE-07's "permanently locked" requirement).
  </action>
  <acceptance_criteria>
    - `npm run test -- src/lib/views/ScoreEntry.test.ts` passes including: "Turnier abschließen" is `disabled` while any cell in the configured tournament is empty; becomes enabled once every shooter/round/passe/arrow cell has a value; clicking it opens a dialog showing `strings.scoring.finalizeModalTitle`; clicking "Ja, abschließen" sets every `db.scores` record's `finalized` to `true` and re-renders all arrow-cell buttons as `disabled`; clicking "Abbrechen" instead leaves every record's `finalized` as `false` and cells remain tappable.
    - `npm run test:e2e -- e2e/scoring.spec.ts` passes both the finalize-then-lock assertion and the finalize-then-reload-still-locked assertion (run at wave-merge/phase-gate frequency, not required on every intermediate save, per RESEARCH.md Sampling Rate).
    - `grep -iE "unlock|wieder.*öffnen|erneut.*bearbeiten" src/lib/views/ScoreEntry.svelte` returns no matches (no unlock/reopen control exists anywhere in the view, per D-10).
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/views/ScoreEntry.test.ts</automated>
  </verify>
  <done>SCORE-06 and SCORE-07 fully satisfied: Abschließen is gated on total completion (every shooter x round x passe x arrow), requires an explicit non-dismissible confirmation, and once confirmed permanently disables every score-entry control with no code path to reverse it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Trainer confirmation click → bulk write locking all tournament data | The single most consequential, irreversible write in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-06 | Tampering | `handleFinalizeConfirm()` — bulk write setting `finalized: true` on all scores | mitigate | Gated behind `isComplete` (every cell filled) AND an explicit non-dismissible `ConfirmDialog` confirmation, matching Phase 2's destructive-action pattern; no programmatic/hidden path sets `finalized` without going through this handler |
| T-03-07 | Repudiation | No audit trail of who/when finalized | accept | Single-trainer, single-device, single-session app — no multi-user attribution requirement (REQUIREMENTS.md explicitly excludes athlete accounts/history) |
| T-03-08 | Elevation of Privilege | Attempting to bypass/unlock `isFinalized` via the UI | mitigate | No unlock control exists anywhere in the codebase (verified by acceptance-criteria grep); `isFinalized` is the sole gate on every score-entry `disabled` attribute, checked reactively via `$derived` from the persisted `db.scores` state |
</threat_model>

<verification>
- `npm run test -- src/lib/utils/scoreCompletion.test.ts src/lib/views/ScoreEntry.test.ts`
- `npm run test:e2e -- e2e/scoring.spec.ts` (phase-gate frequency)
- Manual smoke: fill every cell, click Abschließen, confirm, reload the page, confirm cells remain disabled and no unlock control is visible.
</verification>

<success_criteria>
- All Task 1 and Task 2 automated tests pass, plus the e2e finalize/lock/reload spec.
- SCORE-01 through SCORE-07 are all implemented and covered by tests across Plans 03-01/03-02/03-03.
- No unlock/reopen path exists anywhere in the codebase.
</success_criteria>

<output>
Create `.planning/phases/03-score-entry/03-03-SUMMARY.md` when done
</output>
