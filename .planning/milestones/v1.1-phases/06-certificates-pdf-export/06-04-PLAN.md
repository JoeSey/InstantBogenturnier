---
phase: 06-certificates-pdf-export
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/lib/views/Results.svelte
  - src/lib/components/ResultsTable.svelte
autonomous: true
requirements: [D-01, D-02, D-03, D-04]

must_haves:
  truths:
    - "Trainer can click 'Urkunden erstellen' in the Results view and receive a single ZIP download containing one certificate PDF per shooter across all classes"
    - "Trainer can click a per-row certificate action in the results table and receive a single standalone certificate PDF for just that shooter"
    - "Both actions respect the existing includeIncomplete checkbox state (bulk) / row visibility (per-row) — no separate new filter UI is introduced"
    - "Both actions work with zero network connectivity"
  artifacts:
    - path: "src/lib/views/Results.svelte"
      provides: "handleBulkCertExport() and handleSingleCertExport() handlers, wired to a new 'Urkunden erstellen' button and ResultsTable's per-row callback"
      contains: "generateBulkCerts"
    - path: "src/lib/components/ResultsTable.svelte"
      provides: "per-row certificate action button, exposed as a callback prop"
      contains: "oncertexport"
  key_links:
    - from: "src/lib/views/Results.svelte"
      to: "src/lib/utils/certificateExport.ts"
      via: "generateBulkCerts() / generateSingleCertPdf() imports"
      pattern: "from '../utils/certificateExport'"
    - from: "src/lib/components/ResultsTable.svelte"
      to: "src/lib/views/Results.svelte"
      via: "oncertexport callback prop invoked per row"
      pattern: "oncertexport"
---

<objective>
Wire the two Phase 6 UI entry points into the existing Results view: a tournament-wide "Urkunden erstellen" button (D-01, D-03) next to the existing "PDF exportieren" button, and a per-row certificate action inside `ResultsTable.svelte` (D-02, D-04).

Purpose: This is the only plan where a trainer-visible feature actually appears — Plans 01-03 built the underlying data/logic layer this plan connects to buttons.
Output: Both certificate export paths functional end-to-end, reusing Plan 02's `certificateExport.ts` functions and the Phase 5 WR-04 anchor-download pattern already proven in this same file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-certificates-pdf-export/06-CONTEXT.md
@.planning/phases/06-certificates-pdf-export/06-UI-SPEC.md
</context>

<interfaces>
Full current `src/lib/views/Results.svelte` `handleExport()` (the pattern to replicate twice — reuse verbatim except for the generator function and filename call):
```typescript
async function handleExport() {
  errorFeedback = '';
  try {
    const settings = (await db.settings.get(1)) ?? { id: 1 as const };
    const blob = await generateResultsPdf(rankings, classesWithResults, settings, includeIncomplete);
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = resultsPdfFilename();
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
  } catch {
    errorFeedback = strings.resultsPdf.exportError;
  }
}
```
Existing reactive state already in scope in this file: `rankings` (`$derived(computeClassRankings(...))`, a `Map<number, RankedRow[]>`), `classesWithResults` (`ClassRecord[]`, alphabetical, only classes with results), `includeIncomplete` (`$state(false)`), `errorFeedback` (`$state('')`).

Existing export button markup (lines 173-181) — copy this exact style for the new bulk button, changing only the handler, icon, and label:
```svelte
<button
  type="button"
  onclick={handleExport}
  disabled={classesWithResults.length === 0}
  class="flex min-h-[44px] w-full items-center justify-center gap-2 rounded-lg bg-teal-500 px-4 py-2 text-[16px] font-semibold leading-[1.5] text-white hover:bg-teal-600 disabled:cursor-not-allowed disabled:opacity-50 md:w-auto dark:bg-teal-400 dark:text-slate-900 dark:hover:bg-teal-300"
>
  <FileDown size={20} />
  {strings.resultsPdf.exportButton}
</button>
```
06-UI-SPEC.md's Layout section specifies: both buttons live in the same `GlassCard`, `flex flex-col gap-2 md:flex-row` container, 8px gap.

Full current `src/lib/components/ResultsTable.svelte` — renders one `<table>` with columns Rang/Name/Schießplatz/Gesamt, one `<tr>` per row via `{#each rows as row (row.shooterId)}`. Currently accepts only `let { rows }: { rows: RankedRow[] } = $props();` — no callback props exist yet. 06-UI-SPEC.md specifies a new rightmost "Urkunde" column with a per-row button, 44px touch target, icon-based per Claude's Discretion for mobile.

Available strings (added in Plan 01): `strings.certificateExport.bulkButton` = `'Urkunden erstellen'`, `strings.certificateExport.singleButton` = `'Urkunde'`, `strings.certificateExport.bulkExportError` = `'Urkunden-Export fehlgeschlagen'`, `strings.certificateExport.singleExportError` = `'Urkunden-Export fehlgeschlagen'`.

Plan 02's `certificateExport.ts` exports: `generateBulkCerts(classifications, classes, settings): Promise<Blob>`, `generateSingleCertPdf(rankedRow, className, settings): Promise<Blob>`, `certificatePdfFilename(shooterName, date?): string`, `zipFilename(date?): string`.

`RankedRow` has no `className` field — Results.svelte must pass the class name explicitly when calling `generateSingleCertPdf` per row (it already knows which class each `ResultsTable` instance renders — `cls.name` on desktop, the selected class's name on mobile).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add per-row certificate action to ResultsTable.svelte</name>
  <files>src/lib/components/ResultsTable.svelte</files>
  <read_first>src/lib/components/ResultsTable.svelte, src/lib/i18n/strings.de.ts</read_first>
  <action>
    Import `FileDown` (or another suitable icon already used elsewhere, e.g. `Award`) from `'@lucide/svelte'`. Extend the component's `$props()` destructure to `let { rows, oncertexport }: { rows: RankedRow[]; oncertexport: (row: RankedRow) => void } = $props();` — `oncertexport` is a required callback prop, invoked with the clicked row's `RankedRow`, that the parent (Results.svelte, Task 2) supplies with the class-name-aware handler.

    Add a new `<th>` after the "Gesamt" column header with `strings.results.columnCertificate` as its text (this string does not yet exist — add `columnCertificate: 'Urkunde'` to the existing `results` section of `strings.de.ts` as part of this task, since it is table-column-specific, not covered by Plan 01's `certificateExport`/`settingsForm` additions). Add a matching `<td>` in the row loop containing a `<button type="button" onclick={() => oncertexport(row)}>` styled as a secondary action (text-teal-500, border, hover states per 06-UI-SPEC.md's per-row action spec), `min-h-[44px] min-w-[44px]`, containing the icon plus an `aria-label={strings.certificateExport.singleButton}`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `ResultsTable.svelte` `$props()` destructure includes `oncertexport`
    - `ResultsTable.svelte` template contains a `<button` with `onclick={() => oncertexport(row)}`
    - `strings.de.ts` `results` section contains `columnCertificate`
    - `npm run check` passes
  </acceptance_criteria>
  <done>Each results-table row has a working certificate action button that calls back to the parent with that row's data.</done>
</task>

<task type="auto">
  <name>Task 2: Wire bulk and per-row certificate export handlers in Results.svelte</name>
  <files>src/lib/views/Results.svelte</files>
  <read_first>src/lib/views/Results.svelte, src/lib/utils/certificateExport.ts</read_first>
  <action>
    Import `generateBulkCerts, generateSingleCertPdf, certificatePdfFilename, zipFilename` from `'../utils/certificateExport'`.

    Add `async function handleBulkCertExport()` mirroring `handleExport()`'s try/catch and WR-04 anchor-download structure exactly, but calling `await generateBulkCerts(rankings, classesWithResults, settings)` and downloading with `a.download = zipFilename()`; on catch, set `errorFeedback = strings.certificateExport.bulkExportError`.

    Add `async function handleSingleCertExport(row: RankedRow, className: string)` fetching settings the same way, calling `await generateSingleCertPdf(row, className, settings)`, downloading with `a.download = certificatePdfFilename(row.name)`; on catch, set `errorFeedback = strings.certificateExport.singleExportError`.

    In the template, add a second button immediately after the existing "PDF exportieren" button inside the same `GlassCard`, wrap both buttons in a `<div class="flex flex-col gap-2 md:flex-row">` per 06-UI-SPEC.md's layout spec, using `strings.certificateExport.bulkButton` as its label, `onclick={handleBulkCertExport}`, `disabled={classesWithResults.length === 0}`, same button classes as the existing export button.

    Wire `ResultsTable`'s new `oncertexport` prop at both call sites (mobile single-table render and desktop per-class grid render) to `(row) => handleSingleCertExport(row, /* the class name in scope at that call site */)` — mobile: look up the selected class's name from `classesWithResults`; desktop: `cls.name` is already in scope inside the `{#each classesWithResults as cls}` block.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `Results.svelte` imports `generateBulkCerts` from `'../utils/certificateExport'`
    - `Results.svelte` defines `handleBulkCertExport` and `handleSingleCertExport` functions, both following the WR-04 append-before-click download pattern
    - Both `<ResultsTable>` usages in the template pass an `oncertexport` prop
    - A button with `strings.certificateExport.bulkButton` label exists in the template, disabled when `classesWithResults.length === 0`
    - `npm run check` passes
  </acceptance_criteria>
  <done>Bulk ZIP export and per-row single-certificate export both work from the Results view, using the existing includeIncomplete-filtered rankings and the WR-04 download pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Results view button click → Blob URL → browser download | Same trust boundary already accepted for the Phase 5 "PDF exportieren" button |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-07 | Tampering (Blob URL misuse) | `handleBulkCertExport`/`handleSingleCertExport` | mitigate | `URL.revokeObjectURL(url)` called immediately after `a.click()`, mirroring the existing Phase 5 `handleExport()` pattern exactly — no dangling object URLs |
| T-6-08 | Information Disclosure (wrong class name attached to wrong shooter's certificate) | `handleSingleCertExport(row, className)` call sites | mitigate | className is read from the exact same in-scope `cls`/`classesWithResults` variable the row itself was rendered from — no cross-class lookup, no ambiguity |
</threat_model>

<verification>
- `npm run check` passes
- Manual: click "Urkunden erstellen" → single ZIP downloads containing one PDF per shooter; click a row's certificate button → single PDF downloads for that shooter only
</verification>

<success_criteria>
Both certificate export entry points are live in the Results view, functionally correct, and follow every established Phase 5 pattern (download mechanics, error feedback, button styling).
</success_criteria>

<output>
Create `.planning/phases/06-certificates-pdf-export/06-04-SUMMARY.md` when done
</output>
