---
phase: quick-260705-p25
plan: 01
subsystem: ui
tags: [svelte, tailwind, responsive, score-entry]

# Dependency graph
requires:
  - phase: 03-score-entry
    provides: ScoreTable.svelte component and its sorting/rendering logic
provides:
  - Responsive Klasse-column hiding below the 768px/md breakpoint in ScoreTable.svelte
  - Tightened header/data cell padding on phone widths, restored at md and above
  - Component and e2e test coverage proving the responsive behavior
affects: [phase-04-results]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Tailwind `hidden md:table-cell` pattern for hiding a table column below the app's existing 768px/md breakpoint (matches e2e/nav.spec.ts's sidebar/tab-bar breakpoint convention)"

key-files:
  created: []
  modified:
    - src/lib/components/ScoreTable.svelte
    - src/lib/views/ScoreEntry.test.ts
    - e2e/scoring.spec.ts

key-decisions:
  - "Hid the Klasse column entirely (not just visually compacted) below md, since it was the least essential column for the trainer's live per-arrow tapping workflow at the range"
  - "Left the arrow-picker button's min-h-[44px] min-w-[44px] completely untouched at every breakpoint, only tightening the parent cell's padding, to preserve the WCAG 2.5.5 touch-target floor"

patterns-established:
  - "Phone-width table compaction: hide non-essential columns via `hidden md:table-cell` and tighten cell padding via `p-2 md:p-4` / `px-1.5 py-1.5 md:px-3 md:py-2`, reusable for any other dense data table that needs to fit phone-portrait widths"

requirements-completed: [SCORE-04]

# Metrics
duration: 8min
completed: 2026-07-05
---

# Phase quick-260705-p25: Score table phone-view compaction Summary

**Hid the Klasse column and tightened cell padding in ScoreTable.svelte below the 768px/md breakpoint so the WA 3-arrow preset fits on phone-portrait widths without horizontal scrolling, restoring exactly to prior layout at md and above.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-07-05T18:05:14Z
- **Completed:** 2026-07-05T18:12:31Z
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments
- Klasse `<th>` and `<td>` now carry `hidden md:table-cell`, fully removing the column below 768px and fully restoring it at 768px and above
- All header cells changed from `p-4` to `p-2 md:p-4`, all data cells from `px-3 py-2` to `px-1.5 py-1.5 md:px-3 md:py-2`, tightening phone-width density while leaving `md:`+ visually identical to before
- Arrow-picker button `min-h-[44px] min-w-[44px]` left byte-for-byte unchanged at every breakpoint
- Added a jsdom class-presence test in ScoreEntry.test.ts and two real-browser Playwright assertions (375px hidden / 1024px visible) in scoring.spec.ts

## Task Commits

Each task was committed atomically:

1. **Task 1: Responsive Klasse-column hiding and compact phone padding in ScoreTable.svelte** - `28e4f04` (feat)
2. **Task 2: Component and e2e tests proving Klasse-column responsiveness** - `bde5f40` (test)

**Plan metadata:** committed separately by the orchestrator after this summary.

## Files Created/Modified
- `src/lib/components/ScoreTable.svelte` - Klasse th/td get `hidden md:table-cell`; header/data cell padding tightened below md, restored at md; top comment banner updated to document the new phone-fit strategy
- `src/lib/views/ScoreEntry.test.ts` - New test asserting the Klasse th/td both carry `hidden` and `md:table-cell` classes (static class-presence check, not a real media-query evaluation)
- `e2e/scoring.spec.ts` - New `test.describe('score table phone-view compaction (260705-p25)')` block with two tests: Klasse header hidden at 375px, visible at 1024px

## Decisions Made
- Hid the Klasse column entirely rather than truncating/abbreviating its content, since the trainer's live per-arrow tapping workflow does not need class visibility at the range on a phone
- Kept `overflow-x-auto` as a safety net for narrower devices or larger arrow counts rather than removing it, since it's no longer the primary phone-fit strategy but still useful for edge cases

## Deviations from Plan

None — plan executed exactly as written for both tasks.

### Deferred (out of scope, pre-existing)

**1. Pre-existing `tsc -p tsconfig.node.json` failure in `vite.config.ts` unrelated to this task**
- **Found during:** Task 1 verification (`npm run check`)
- **Issue:** `vite.config.ts(5,54): error TS2307: Cannot find module './src/lib/config/app.config'` — caused by `tsconfig.node.json`'s `"module": "nodenext"` + `allowImportingTsExtensions` requiring an explicit `.ts` extension on the relative import, which `vite.config.ts` omits.
- **Verified pre-existing:** Reproduced identically by checking out the original (pre-task) `ScoreTable.svelte` and re-running `npm run check` — same failure, confirming it predates this quick task (traces back to the Phase 1 scaffold commit `2954711`).
- **Scope:** Entirely unrelated to `ScoreTable.svelte`, the only file this task modifies. The `svelte-check` portion of `npm run check` (which does cover `.svelte` files) passes with 0 errors/warnings; only the separate `tsc -p tsconfig.node.json` step fails.
- **Not fixed:** Out of scope per executor scope-boundary rule. Logged to `.planning/deferred-items.md`.

---

**Total deviations:** 0 auto-fixed; 1 deferred (pre-existing, out of scope)
**Impact on plan:** No scope creep. Plan's Task 1 `<done>` criterion for svelte-check (the portion actually covering the modified file) is satisfied; the unrelated `tsc -p tsconfig.node.json` failure predates and is independent of this change.

## Issues Encountered
- A stale `vite preview` server was left running on port 4173 from a prior session, serving an outdated build to Playwright and causing the first e2e run of the new phone-compaction tests to fail (`toBeHidden()` saw the old `p-4`-only class list). Killed the stale process; Playwright's `webServer` config rebuilt fresh and all 4 tests (2 pre-existing + 2 new) passed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 4 (results) is unaffected by this change; ScoreTable.svelte's ranking-relevant data (`ScoreRow.className`, arrows, sum) is untouched — only display-layer CSS classes changed.
- No blockers or concerns raised by this quick task.

---
*Phase: quick-260705-p25*
*Completed: 2026-07-05*

## Self-Check: PASSED

All created/modified files and both task commit hashes verified present.
