---
phase: 06-certificates-pdf-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/db/schema.ts
  - src/lib/db/schema.test.ts
  - src/lib/i18n/strings.de.ts
autonomous: true
requirements: [D-03, D-05]

must_haves:
  truths:
    - "JSZip is installed and importable from application code"
    - "SettingsRecord has a certificateHeading field that defaults to 'Urkunde' for existing settings rows after upgrade"
    - "All Phase 6 UI copy (buttons, labels, errors) exists in the central strings module before any component references it"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "SettingsRecord.certificateHeading field + Dexie v5 migration with upgrade() default"
      contains: "certificateHeading"
    - path: "package.json"
      provides: "jszip runtime dependency"
      contains: "jszip"
  key_links:
    - from: "src/lib/db/schema.ts"
      to: "src/lib/utils/certificateExport.ts (Plan 02)"
      via: "SettingsRecord type import"
      pattern: "certificateHeading"
---

<objective>
Lay the foundation Phase 6 needs before any certificate-specific logic is written: install the JSZip dependency (D-03), extend the Dexie schema with a `certificateHeading` singleton field via a v5 migration (D-05), and add every new German UI string this phase will need in one place so downstream plans only ever import from `strings.de.ts`, never edit it.

Purpose: Every other Phase 6 plan (certificateExport.ts, SettingsForm.svelte, Results.svelte/ResultsTable.svelte) depends on either the `jszip` package, the `certificateHeading` schema field, or a strings.de.ts key existing already — doing all three here in Wave 1 lets Wave 2 plans run in parallel with zero file overlap on `strings.de.ts`.
Output: `jszip` installed; `SettingsRecord` and Dexie v5 migration in place with passing upgrade test; `certificateExport` and `settingsForm` string sections extended in `strings.de.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-certificates-pdf-export/06-CONTEXT.md
@.planning/phases/06-certificates-pdf-export/06-RESEARCH.md
@.planning/phases/06-certificates-pdf-export/06-UI-SPEC.md
</context>

<interfaces>
Current `SettingsRecord` (src/lib/db/schema.ts, lines 77-82):
```typescript
export interface SettingsRecord {
  id: 1;
  title?: string;
  logoLeftBlob?: Blob;
  logoRightBlob?: Blob;
}
```

Current v4 Dexie version block (src/lib/db/schema.ts, lines 118-126) — must be restated unchanged inside the new v5 block per Dexie's versioning requirement, only `.upgrade()` is new:
```typescript
this.version(4).stores({
  classes: '++id, name',
  shootingLines: 'id',
  rounds: 'id',
  shooters: '++id, classId, lineAssignment',
  presets: '++id, name',
  scores: '[shooterId+roundIndex+passeIndex+arrowIndex], shooterId, roundIndex',
  settings: 'id',
});
```

Existing v4 test block pattern (src/lib/db/schema.test.ts, lines 94-119) — mirror this shape exactly for the new v5 describe block, changing only the migration-specific assertions.

Existing `resultsPdf` and `settingsForm` string sections (src/lib/i18n/strings.de.ts, end of file) — the new `certificateExport` section is a new top-level sibling key; `certificateHeadingLabel`/`certificateHeadingPlaceholder` are two new keys appended inside the existing `settingsForm` object.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install JSZip and add the certificateHeading schema migration</name>
  <files>package.json, package-lock.json, src/lib/db/schema.ts, src/lib/db/schema.test.ts</files>
  <read_first>src/lib/db/schema.ts, src/lib/db/schema.test.ts, package.json</read_first>
  <action>
    Run `npm install jszip` to add it as a runtime dependency (RESEARCH.md confirms jszip 3.10.1 passed the Package Legitimacy Audit — no checkpoint required).

    In `src/lib/db/schema.ts`: add `certificateHeading?: string;` to the `SettingsRecord` interface (per D-05). Add a `this.version(5).stores({...})` block directly after the existing `this.version(4)` block, restating all v4 store definitions unchanged (classes, shootingLines, rounds, shooters, presets, scores, settings — identical strings to v4), and chain `.upgrade(tx => tx.table('settings').toCollection().modify(record => { if (!record.certificateHeading) { record.certificateHeading = 'Urkunde'; } }))`. Update the code comment above the v4 block region to note v5 exists (mirror the existing comment style used for v3/v4 — "every prior version's stores must be restated unchanged").

    In `src/lib/db/schema.test.ts`: add a new `describe('Dexie v5 schema', ...)` block mirroring the existing v4 block's `beforeEach(async () => { await resetDb(); })` setup and the "defines all 7 tables including settings" test. Add two new v5-specific tests: (1) putting a settings row without `certificateHeading` then reading it back returns `certificateHeading === 'Urkunde'` after the v5 upgrade runs, (2) putting a settings row with an explicit `certificateHeading: 'Teilnahmeurkunde'` round-trips that exact string unchanged.
  </action>
  <verify>
    <automated>npm test -- src/lib/db/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` contains `"jszip"` and `npm ls jszip` exits 0
    - `src/lib/db/schema.ts` `SettingsRecord` interface contains the string `certificateHeading?: string`
    - `src/lib/db/schema.ts` contains a `this.version(5)` block with `.upgrade(` chained after `.stores(`
    - `npm test -- src/lib/db/schema.test.ts` passes, including a v5 describe block with at least 2 new test cases
  </acceptance_criteria>
  <done>jszip is an installed dependency; SettingsRecord.certificateHeading exists; Dexie v5 migration initializes it to 'Urkunde' for existing rows; schema.test.ts v5 block passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 6 UI strings</name>
  <files>src/lib/i18n/strings.de.ts</files>
  <read_first>src/lib/i18n/strings.de.ts, .planning/phases/06-certificates-pdf-export/06-UI-SPEC.md</read_first>
  <action>
    Append a new top-level `certificateExport` section to the `strings` object (after the existing `settingsForm` section, following the same `as const` object literal style) with exactly these keys and values from 06-UI-SPEC.md's Copywriting Contract: `bulkButton: 'Urkunden erstellen'`, `singleButton: 'Urkunde'`, `bulkExportError: 'Urkunden-Export fehlgeschlagen'`, `singleExportError: 'Urkunden-Export fehlgeschlagen'`, `zipCreationError: 'Zip-Datei konnte nicht erstellt werden'`.

    Extend the existing `settingsForm` object (do not create a new section) with two new keys placed after `logoSizeHint`: `certificateHeadingLabel: 'Urkunden-Überschrift (für PDF)'`, `certificateHeadingPlaceholder: "z.B. 'Urkunde' oder 'Teilnahmeurkunde'"`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `strings.de.ts` exports a `certificateExport` object with keys `bulkButton`, `singleButton`, `bulkExportError`, `singleExportError`, `zipCreationError`
    - `strings.de.ts` `settingsForm` object contains `certificateHeadingLabel` and `certificateHeadingPlaceholder`
    - `grep -c certificateHeadingLabel src/lib/i18n/strings.de.ts` returns 1 (single definition, no duplication)
  </acceptance_criteria>
  <done>All Phase 6 UI copy exists in strings.de.ts; no downstream plan needs to add new string keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → build | Third-party package (jszip) enters the dependency tree and ships in the client bundle |
| IndexedDB settings row → jsPDF text rendering (Plan 02) | certificateHeading free-text field, set here as a schema default, consumed later by PDF drawing code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | jszip npm install | accept | RESEARCH.md Package Legitimacy Audit already verified jszip 3.10.1 as [OK] (mature, 30M/week downloads, no slopcheck flags) — no blocking checkpoint required per gate rule for approved packages |
| T-6-02 | Tampering | Dexie v5 upgrade() function | mitigate | Upgrade only ever sets a string default; no user input processed during migration, no code execution path from stored data |
</threat_model>

<verification>
- `npm test -- src/lib/db/schema.test.ts` passes (v4 and v5 blocks both green)
- `npm run check` passes (no TypeScript errors from the new SettingsRecord field or strings additions)
- `npm ls jszip` resolves without error
</verification>

<success_criteria>
jszip installed; schema v5 migration in place and tested; all Phase 6 strings available for Plans 02-05 to consume without further edits to strings.de.ts.
</success_criteria>

<output>
Create `.planning/phases/06-certificates-pdf-export/06-01-SUMMARY.md` when done
</output>
</content>
