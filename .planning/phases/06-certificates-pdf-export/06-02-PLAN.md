---
phase: 06-certificates-pdf-export
plan: 02
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/lib/utils/certificateExport.ts
  - src/lib/utils/certificateExport.test.ts
autonomous: true
requirements: [D-01, D-03, D-04, D-05, D-06, D-07, D-08]

must_haves:
  truths:
    - "A single shooter's data (name, class, rank, sum) can be turned into a one-page portrait A4 jsPDF document reusing the Phase 5 header/logo treatment"
    - "The certificate heading text comes from settings.certificateHeading, defaulting to 'Urkunde' when unset"
    - "All shooters with a result in a class can be bundled into a single ZIP Blob, one PDF per shooter, with no top-N cutoff"
    - "Filenames match the exact D-08 conventions for both the ZIP and per-shooter PDFs"
  artifacts:
    - path: "src/lib/utils/certificateExport.ts"
      provides: "buildCertPdf, generateSingleCertPdf, generateBulkCerts, certificatePdfFilename, zipFilename — pure, framework-free functions"
      exports: ["buildCertPdf", "generateSingleCertPdf", "generateBulkCerts", "certificatePdfFilename", "zipFilename"]
    - path: "src/lib/utils/certificateExport.test.ts"
      provides: "Unit tests covering CRT-01 through CRT-06, CRT-10 from 06-RESEARCH.md's Test Map"
      min_lines: 60
  key_links:
    - from: "src/lib/utils/certificateExport.ts"
      to: "src/lib/utils/pdfExport.ts"
      via: "containFit()-equivalent logo aspect-ratio scaling"
      pattern: "containFit"
    - from: "src/lib/utils/certificateExport.ts"
      to: "jszip"
      via: "import JSZip from 'jszip'"
      pattern: "import JSZip"
---

<objective>
Implement the pure, framework-free certificate-generation core: `buildCertPdf()` (single-shooter A4 portrait PDF reusing Phase 5's header/logo rendering), `generateSingleCertPdf()` (Blob wrapper), `generateBulkCerts()` (loops all shooters into a JSZip archive), and the two filename helpers `certificatePdfFilename()` / `zipFilename()` — all per D-01, D-03 through D-08.

Purpose: This is the single reusable module both UI entry points (bulk "Urkunden erstellen" and per-row single export, Plan 04) call into. Building and testing it in isolation — no Svelte dependency — mirrors `pdfExport.ts`'s and `ranking.ts`'s established pure-function style, keeping certificate logic unit-testable without a browser.
Output: `src/lib/utils/certificateExport.ts` with all five exports; `src/lib/utils/certificateExport.test.ts` with passing tests for filename formatting, single-certificate page count/content, and ZIP file count/naming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-certificates-pdf-export/06-CONTEXT.md
@.planning/phases/06-certificates-pdf-export/06-RESEARCH.md
@.planning/phases/06-certificates-pdf-export/06-PATTERNS.md
@.planning/phases/06-certificates-pdf-export/06-UI-SPEC.md
</context>

<interfaces>
Full current `src/lib/utils/pdfExport.ts` (reuse verbatim, do not reimplement):
```typescript
function blobToDataUri(blob: Blob): Promise<string> {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => resolve(reader.result as string);
    reader.onerror = () => reject(reader.error);
    reader.readAsDataURL(blob);
  });
}

export function containFit(
  naturalWidth: number,
  naturalHeight: number,
  maxWidth: number,
  maxHeight: number
): { width: number; height: number } {
  if (!naturalWidth || !naturalHeight) {
    return { width: maxWidth, height: maxHeight };
  }
  const ratio = Math.min(maxWidth / naturalWidth, maxHeight / naturalHeight);
  return { width: naturalWidth * ratio, height: naturalHeight * ratio };
}
```
Note: `blobToDataUri` is not exported from `pdfExport.ts`. Either import `containFit` from `pdfExport.ts` (it IS exported) and re-implement the small `blobToDataUri` helper locally in `certificateExport.ts` (same 6-line body, same behavior), or add `export` to `blobToDataUri` in `pdfExport.ts` if the executor judges that cleaner — either is acceptable, but do not diverge in behavior.

`RankedRow` (src/lib/utils/ranking.ts, lines 11-18):
```typescript
export interface RankedRow {
  shooterId: number;
  name: string;
  line: number | null;
  sum: number;
  rank: number;
  isComplete: boolean;
}
```
Note: `RankedRow` has no `classId`/`className` field — the class name must be passed in separately by the caller (per-class loop), exactly as `buildResultsPdfDoc()` in `pdfExport.ts` does with its `classes: ClassRecord[]` parameter.

`SettingsRecord` (src/lib/db/schema.ts, post-Plan-01):
```typescript
export interface SettingsRecord {
  id: 1;
  title?: string;
  logoLeftBlob?: Blob;
  logoRightBlob?: Blob;
  certificateHeading?: string;
}
```

`ClassRecord` (src/lib/db/schema.ts):
```typescript
export interface ClassRecord {
  id?: number;
  name: string;
  ageGroup?: string;
  bowType?: string;
  distance?: string;
}
```

`resultsPdfFilename()` pattern to mirror for the two new filename functions (src/lib/utils/pdfExport.ts, lines 10-12):
```typescript
export function resultsPdfFilename(date: Date = new Date()): string {
  return `Ergebnisse_${date.toISOString().split('T')[0]}.pdf`;
}
```

`buildResultsPdfDoc()`'s header rendering block (src/lib/utils/pdfExport.ts, lines 84-116) — reuse this exact logic (logo placement via `containFit`, title text) as the header of each certificate page, just with different content below it.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for certificateExport.ts (RED)</name>
  <files>src/lib/utils/certificateExport.test.ts</files>
  <read_first>src/lib/utils/pdfExport.test.ts, src/lib/utils/ranking.ts, .planning/phases/06-certificates-pdf-export/06-RESEARCH.md</read_first>
  <behavior>
    - `certificatePdfFilename('Max Mustermann', new Date('2026-07-06T10:00:00Z'))` returns exactly `'Urkunde_Max Mustermann_2026-07-06.pdf'`
    - `zipFilename(new Date('2026-07-06T10:00:00Z'))` returns exactly `'Urkunden_2026-07-06.zip'`
    - `buildCertPdf(rankedRow, className, settings)` returns a jsPDF document where `doc.getNumberOfPages()` is exactly 1
    - `buildCertPdf(...)` renders the certificate heading text from `settings.certificateHeading` when set, and falls back to `'Urkunde'` when `settings.certificateHeading` is undefined
    - `generateSingleCertPdf(...)` returns a `Blob` with `type` starting with `application/pdf`
    - `generateBulkCerts(classifications, classes, settings)` returns a `Blob`; when unzipped (via `JSZip.loadAsync`), the archive contains exactly one file per shooter across all classes in `classifications`, and each filename matches `/^Urkunde_.+_\d{4}-\d{2}-\d{2}\.pdf$/`
    - `generateBulkCerts(...)` includes all shooters in a class's `RankedRow[]` (no top-N cutoff, per D-01) — a 5-shooter class produces 5 files
  </behavior>
  <action>
    Follow `pdfExport.test.ts`'s structure: import from `./certificateExport`, reuse a local `makeRow(overrides: Partial&lt;RankedRow&gt;): RankedRow` helper identical to `pdfExport.test.ts`'s (shooterId, name, line, sum, rank, isComplete). Write `describe` blocks for `certificatePdfFilename`, `zipFilename`, `buildCertPdf`, `generateSingleCertPdf`, and `generateBulkCerts` covering the behaviors listed above. For the ZIP content assertion, import `JSZip` directly in the test file and call `JSZip.loadAsync(blob)` then inspect `Object.keys(zip.files)`. Run the test file now — every test MUST fail (certificateExport.ts does not exist yet).
  </action>
  <verify>
    <automated>npm test -- src/lib/utils/certificateExport.test.ts 2>&1 | grep -q "FAIL\|Cannot find module" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/utils/certificateExport.test.ts` exists with at least 5 `describe` blocks matching the behaviors above
    - Running the test file fails because `./certificateExport` does not yet exist (module-not-found or all tests failing)
  </acceptance_criteria>
  <done>Failing test file committed, describing exact expected behavior of every exported function before implementation exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement certificateExport.ts to pass all tests (GREEN)</name>
  <files>src/lib/utils/certificateExport.ts</files>
  <read_first>src/lib/utils/certificateExport.test.ts, src/lib/utils/pdfExport.ts, src/lib/db/schema.ts</read_first>
  <action>
    Create `src/lib/utils/certificateExport.ts` importing `jsPDF` from `'jspdf'`, `JSZip` from `'jszip'`, `containFit` from `'./pdfExport'`, `type { RankedRow }` from `'./ranking'`, and `type { ClassRecord, SettingsRecord }` from `'../db/schema'`. Add the same framework-free code comment style as `pdfExport.ts` line 6-8 noting this module mirrors that pattern.

    Implement `certificatePdfFilename(shooterName: string, date: Date = new Date()): string` returning `` `Urkunde_${shooterName}_${date.toISOString().split('T')[0]}.pdf` `` (D-08). Implement `zipFilename(date: Date = new Date()): string` returning `` `Urkunden_${date.toISOString().split('T')[0]}.zip` `` (D-08).

    Implement `async function buildCertPdf(rankedRow: RankedRow, className: string, settings: Pick&lt;SettingsRecord, 'title' | 'logoLeftBlob' | 'logoRightBlob' | 'certificateHeading'&gt; | undefined): Promise&lt;jsPDF&gt;` — `new jsPDF({ orientation: 'portrait', unit: 'mm', format: 'a4' })` (D-07), reuse the exact header rendering block from `pdfExport.ts` lines 84-116 (logos via `containFit`, title text) verbatim, then below it render the certificate heading (`settings?.certificateHeading ?? 'Urkunde'`, 28pt bold centered per 06-UI-SPEC.md's PDF Output Styling section), then shooter name (16pt bold centered), `Klasse: ${className}` / `Platzierung: ${rankedRow.rank}` / `Punkte: ${rankedRow.sum}` (11pt regular centered, per D-06) at the mm offsets specified in 06-UI-SPEC.md's "Certificate Content Structure" section. Export `buildCertPdf`.

    Implement `async function generateSingleCertPdf(rankedRow, className, settings): Promise&lt;Blob&gt;` wrapping `buildCertPdf(...).output('blob')`.

    Implement `async function generateBulkCerts(classifications: Map&lt;number, RankedRow[]&gt;, classes: ClassRecord[], settings: Pick&lt;SettingsRecord, 'title' | 'logoLeftBlob' | 'logoRightBlob' | 'certificateHeading'&gt; | undefined): Promise&lt;Blob&gt;` — filter+sort `classes` by `cls.id !== undefined && classifications.has(cls.id)` then alphabetically by name (same ordering as `buildResultsPdfDoc`), loop each class's `RankedRow[]` with no top-N cutoff (D-01), call `generateSingleCertPdf` per row, `zip.file(certificatePdfFilename(row.name), blob)`, return `zip.generateAsync({ type: 'blob', compression: 'DEFLATE' })`.

    Run the full test file until all tests pass (GREEN). Do not modify the test file to make it pass — only implementation code.
  </action>
  <verify>
    <automated>npm test -- src/lib/utils/certificateExport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/lib/utils/certificateExport.test.ts` passes with 0 failures
    - `certificateExport.ts` exports exactly: `buildCertPdf`, `generateSingleCertPdf`, `generateBulkCerts`, `certificatePdfFilename`, `zipFilename`
    - `grep -c "containFit" src/lib/utils/certificateExport.ts` >= 1 (confirms Phase 5 logo scaling is reused, not reimplemented)
    - `npm run check` passes (no TypeScript errors)
  </acceptance_criteria>
  <done>All tests from Task 1 pass; certificate PDF/ZIP generation functions are implemented, framework-free, and reuse Phase 5's containFit/header pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dexie settings.certificateHeading → jsPDF text rendering | Free-text field flows into `doc.text()` calls |
| RankedRow[] (many shooters) → JSZip archive | Bulk loop assembling potentially dozens of Blobs into memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03 | Tampering (script injection via certificateHeading) | `buildCertPdf()` `doc.text()` calls | accept | jsPDF renders all `doc.text()` input as literal vector glyphs, never interpreted as HTML/JS — confirmed in 06-RESEARCH.md Security Domain; no HTML/DOM sink exists in this module |
| T-6-04 | Denial of Service (large batch memory exhaustion) | `generateBulkCerts()` loop | accept | Typical tournament scale (20-60 shooters) produces a ~5-10MB ZIP; RESEARCH.md Pitfall 3 confirms no issue below ~100 shooters; no batching added per Claude's Discretion (not requested, would add unrequested complexity) |
| T-6-05 | Tampering | ZIP filename collisions on duplicate shooter names | accept | Per D-08 filenames are name+date only; duplicate-name collision is a known, low-likelihood edge case (RESEARCH.md Pitfall 5) not covered by any locked decision — out of scope for this plan |
</threat_model>

<verification>
- `npm test -- src/lib/utils/certificateExport.test.ts` passes
- `npm run check` passes with no new TypeScript errors
</verification>

<success_criteria>
`certificateExport.ts` provides fully tested, pure functions for single-certificate and bulk-ZIP generation that Plan 04's UI wiring can call directly with no further logic needed.
</success_criteria>

<output>
Create `.planning/phases/06-certificates-pdf-export/06-02-SUMMARY.md` when done
</output>
