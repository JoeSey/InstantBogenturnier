---
phase: 02-setup-registration
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/utils/modeDetection.ts
  - src/lib/utils/modeDetection.test.ts
  - src/lib/utils/shooterAutoAssignment.ts
  - src/lib/utils/shooterAutoAssignment.test.ts
  - src/lib/components/ShooterForm.svelte
  - src/lib/components/ShooterForm.test.ts
  - src/lib/components/AutoAssignModal.svelte
  - src/lib/views/Registration.svelte
  - src/App.svelte
autonomous: true
requirements: [REG-01, REG-02]
user_setup: []

must_haves:
  truths:
    - "Trainer can register a shooter with a name, a required class assignment, and an optional shooting-line assignment."
    - "If the trainer leaves the line blank, the app auto-assigns it (round-robin, balanced across lines/flights) and shows a transparent preview before committing."
    - "The app displays whether the tournament is running in AB or AB/CD mode, computed from registered shooter count vs. configured line count, and this updates as shooters are added."
  artifacts:
    - path: "src/lib/utils/modeDetection.ts"
      provides: "detectMode(shooterCount, lineCount) pure function"
      exports: ["detectMode", "TournamentMode"]
    - path: "src/lib/utils/shooterAutoAssignment.ts"
      provides: "assignShootersToLines, previewAssignmentSummary pure functions"
      exports: ["assignShootersToLines", "previewAssignmentSummary", "LineAssignment"]
    - path: "src/lib/views/Registration.svelte"
      provides: "Registration view with mode indicator, shooter form, and shooter list; replaces RegistrationPlaceholder"
    - path: "src/lib/components/AutoAssignModal.svelte"
      provides: "Non-dismissible auto-assignment preview modal per UI-SPEC"
  key_links:
    - from: "src/lib/views/Registration.svelte"
      to: "src/lib/utils/modeDetection.ts"
      via: "import { detectMode }"
      pattern: "detectMode\\("
    - from: "src/lib/components/ShooterForm.svelte"
      to: "src/lib/components/AutoAssignModal.svelte"
      via: "opens modal on submit before persisting"
      pattern: "AutoAssignModal"
    - from: "src/lib/components/AutoAssignModal.svelte"
      to: "db.shooters"
      via: "db.shooters.bulkAdd on confirm"
      pattern: "db\\.shooters\\.(bulkAdd|update)"
---

## Phase Goal

**As a** trainer, **I want to** configure classes, shooting lines, rounds/passes, and register shooters (reusing saved presets), **so that** the tournament is fully set up and ready for live scoring without redoing configuration for every event.

<objective>
Deliver the shooter registration vertical slice: name/class/optional-line form, transparent auto-assignment preview modal, live AB/AB-CD mode indicator, and a responsive shooter list with edit/delete.

Purpose: REG-01 and REG-02 complete the Registration half of Phase 2. Depends only on Plan 01's schema (classes + shootingLines + shooters tables) — does not depend on Plan 02 (rounds/lines UI), since it only reads the already-defined shootingLines table directly.
Output: Working Registration view wired into App.svelte, replacing RegistrationPlaceholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-setup-registration/02-CONTEXT.md
@.planning/phases/02-setup-registration/02-RESEARCH.md
@.planning/phases/02-setup-registration/02-UI-SPEC.md
@.planning/phases/02-setup-registration/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Consumed from Plan 01 — no re-derivation needed. -->

From src/lib/db/schema.ts (Plan 01):
- `ShooterRecord { id?: number; name: string; classId: number; lineAssignment?: number | null; flight?: 'A/B' | 'C/D' | null }` — table `shooters: '++id, classId, lineAssignment'`
- `ClassRecord` — for the class dropdown
- `ShootingLineConfig` — `db.shootingLines.get(1)` for mode detection's `lineCount`
- `resetDb()` from src/lib/db/testHelpers.ts — call in every new test file's `beforeEach`

From src/lib/i18n/strings.de.ts (Plan 01, already fully populated):
- `strings.registration.*` — all shooter-form, mode-indicator, table, and auto-assign-modal copy (see 02-UI-SPEC.md's Strings Module Extension, `registration` section)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mode detection & auto-assignment logic</name>
  <files>src/lib/utils/modeDetection.ts, src/lib/utils/modeDetection.test.ts, src/lib/utils/shooterAutoAssignment.ts, src/lib/utils/shooterAutoAssignment.test.ts</files>
  <read_first>.planning/phases/02-setup-registration/02-CONTEXT.md (D-08, D-09, D-10), .planning/phases/02-setup-registration/02-RESEARCH.md ("AB/AB-CD mode detection" and "Shooter Auto-Assignment (Round-Robin)" code examples), src/lib/db/schema.ts (ShooterRecord shape)</read_first>
  <behavior>
    - `detectMode(4, 2)` returns `'AB'` (4 is not greater than 2 × 2)
    - `detectMode(5, 2)` returns `'AB/CD'` (5 > 4)
    - `detectMode(0, 3)` returns `'AB'`
    - `assignShootersToLines(4, 2, 'AB')` returns line numbers `[1,2,1,2]`, each with `flight: 'A/B'`
    - `assignShootersToLines(5, 2, 'AB/CD')` returns line numbers `[1,2,1,2,1]` with flights `['A/B','A/B','C/D','C/D','A/B']` (flight flips every full pass through all lines: `Math.floor(index / lineCount) % 2 === 1 ? 'C/D' : 'A/B'`)
    - `previewAssignmentSummary(assignments)` returns a comma-joined string of line numbers, e.g. `'1,2,1,2,1'`
  </behavior>
  <action>
    Create modeDetection.ts exporting `type TournamentMode = 'AB' | 'AB/CD'` and `detectMode(shooterCount: number, lineCount: number): TournamentMode` implementing `shooterCount > 2 * lineCount` per D-09. Create shooterAutoAssignment.ts exporting `interface LineAssignment { lineNum: number; flight: 'A/B' | 'C/D' }`, `assignShootersToLines(unassignedCount: number, lineCount: number, mode: TournamentMode): LineAssignment[]` implementing round-robin `(index % lineCount) + 1` for `lineNum` (per D-10) and the flight formula above, and `previewAssignmentSummary(assignments: LineAssignment[]): string` joining `lineNum` values with commas.
  </action>
  <acceptance_criteria>
    - `npm run test -- src/lib/utils/modeDetection.test.ts src/lib/utils/shooterAutoAssignment.test.ts` passes all 6 behavior cases listed above
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/utils/modeDetection.test.ts src/lib/utils/shooterAutoAssignment.test.ts</automated>
  </verify>
  <done>Both pure-logic modules exist, are exported, and are fully unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Shooter registration vertical slice with auto-assignment preview</name>
  <files>src/lib/components/ShooterForm.svelte, src/lib/components/ShooterForm.test.ts, src/lib/components/AutoAssignModal.svelte, src/lib/views/Registration.svelte, src/App.svelte</files>
  <read_first>src/lib/utils/modeDetection.ts, src/lib/utils/shooterAutoAssignment.ts (Task 1), src/lib/i18n/strings.de.ts (registration section, populated in 02-01), src/lib/db/schema.ts, src/lib/views/RegistrationPlaceholder.svelte, src/App.svelte, src/lib/components/GlassCard.svelte, src/lib/components/UpdateBanner.svelte (dialog conditional-render analog), .planning/phases/02-setup-registration/02-UI-SPEC.md (Registration Copy table; "Registration View" layout; "Auto-Assignment Preview Modal" section; "Shooter Registration Auto-Assignment" interaction steps)</read_first>
  <action>
    Build ShooterForm.svelte: `name` `$state` (required, trimmed non-empty), `classId` `$state` select populated from `liveQuery(() => db.classes.toArray())` (required — block submit if unset), optional `lineNum` `$state` number input (blank means auto-assign). On submit, validate name+classId (silent no-op if invalid, matching ClassForm's pattern); if valid, stage the new shooter in-memory (do not write to `db.shooters` yet) and open `AutoAssignModal` with the full set of already-registered-but-unassigned shooters plus the new staged one. Build AutoAssignModal.svelte (non-dismissible, glass-card overlay per UI-SPEC "Modals & Overlays"): props include the staged/unassigned roster and `lineCount`/`mode`; computes assignments via `assignShootersToLines()`, renders `strings.registration.autoAssignModalBody` with `{count}`, the line list via `strings.registration.autoAssignModalLines` and `previewAssignmentSummary()`, and the rationale text `strings.registration.autoAssignModalRationale`. Two footer buttons: "Speichern" (`strings.registration.autoAssignSaveButton`) commits — shooters with a manually-entered `lineNum` keep it as-is (with `flight: null`), blank ones get the computed `LineAssignment` — via `db.shooters.bulkAdd(...)`; "Zurück" (`strings.registration.autoAssignBackButton`) closes the modal and discards the staged entry, returning focus to the form. Build Registration.svelte: mode indicator card computing `shooterCount` from `liveQuery(() => db.shooters.toArray()).length` and `lineCount` from `liveQuery(() => db.shootingLines.get(1))?.count ?? 2`, both wrapped in `$derived`, feeding `detectMode(shooterCount, lineCount)`; renders `strings.registration.modeAB`/`modeABCD` + explanation text. Below, `ShooterForm`, then the shooter list: an HTML `<table>` (Name/Klasse/Schießplatz columns + `Pencil`/`Trash2` icon actions) on ≥768px, glass-card list on <768px (per UI-SPEC responsive breakpoint), and the empty state (`strings.registration.emptyHeading`/`emptyBody`) when no shooters exist. Clicking the edit (`Pencil`) icon pre-fills `ShooterForm` with the selected shooter's values; the form's submit path updates via `db.shooters.update(id, {...})` instead of opening the auto-assign modal when editing an existing shooter (line reassignment on edit is direct, no auto-assign preview needed since it's a single-shooter correction, not new registration). Clicking `Trash2` calls `db.shooters.delete(id)` directly (no confirm — matches the lighter-weight pattern already used for classes; presets get the heavier confirm treatment in Plan 04). Wire `Registration` into App.svelte, replacing `RegistrationPlaceholder`.
  </action>
  <acceptance_criteria>
    - ShooterForm.test.ts: submitting with an empty name is a no-op — no modal opens, `db.shooters.count()` stays 0
    - ShooterForm.test.ts: submitting name="Anna", a selected classId, and a blank line opens `AutoAssignModal` showing exactly 1 assignment; clicking "Speichern" persists exactly one `db.shooters` record with a computed `lineAssignment` and `flight`
    - ShooterForm.test.ts (or an adjacent Registration-level test in the same file): registering enough shooters to cross `shooterCount > 2 × lineCount` flips the rendered mode text from "Modus: AB" to "Modus: AB/CD"
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/components/ShooterForm.test.ts</automated>
  </verify>
  <done>Trainer can register a shooter end-to-end (form → review modal → persisted record visible in the shooter list); the mode indicator reflects live shooter/line counts; edit and delete work from the shooter list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Trainer input → ShooterForm state → db.shooters | Untrusted shooter names enter via free text |
| Auto-assignment algorithm output → displayed to trainer before commit | Purely computational, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-02-06 | Tampering | ShooterForm.svelte (name, classId, lineAssignment inputs) | mitigate | Required-field validation (non-empty trimmed name; `classId` must reference an existing `db.classes` record via the populated dropdown); Svelte's template auto-escaping prevents stored-markup injection via shooter names |
| T-02-07 | Information Disclosure | AB/AB-CD mode & auto-assignment algorithm exposed in the UI | accept | Purely local computational logic with no sensitive data; transparency is an explicit UX requirement (RESEARCH.md Pitfall 4), not a security concern |
</threat_model>

<verification>
Run `npm run test -- src/lib/utils/modeDetection.test.ts src/lib/utils/shooterAutoAssignment.test.ts src/lib/components/ShooterForm.test.ts` — must be green before this plan is considered done.
</verification>

<success_criteria>
- Shooter registration works end-to-end with transparent, reviewable auto-assignment.
- AB/AB-CD mode indicator is correct and updates live as shooters are registered.
</success_criteria>

<output>
Create `.planning/phases/02-setup-registration/02-03-SUMMARY.md` when done
</output>
