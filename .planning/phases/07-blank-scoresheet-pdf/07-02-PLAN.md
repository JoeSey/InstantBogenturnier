---
phase: 07-blank-scoresheet-pdf
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/lib/views/SetupRounds.svelte
  - src/lib/i18n/strings.de.ts
  - e2e/scoresheetExport.spec.ts
autonomous: true
requirements: [SHEET-01, SHEET-07]
must_haves:
  truths:
    - "Trainer can click a button in the Einrichtung (Setup) view, next to the rounds/passes config, that downloads a scoresheet PDF"
    - "Download works with zero network connectivity"
    - "Downloaded filename matches Schießformular_YYYY-MM-DD.pdf"
  artifacts:
    - path: "src/lib/views/SetupRounds.svelte"
      provides: "Scoresheet download button wired to generateScoresheetPdf"
    - path: "e2e/scoresheetExport.spec.ts"
      provides: "E2E coverage of the download flow, including offline mode"
  key_links:
    - from: "src/lib/views/SetupRounds.svelte"
      to: "src/lib/utils/scoresheetExport.ts"
      via: "generateScoresheetPdf, scoresheetPdfFilename imports"
      pattern: "generateScoresheetPdf"
    - from: "src/lib/views/SetupRounds.svelte"
      to: "src/lib/i18n/strings.de.ts"
      via: "strings.scoresheetExport.*"
      pattern: "strings\\.scoresheetExport"
---

<objective>
Wire the scoresheet PDF generator (built in Plan 01) into the Einrichtung (Setup) view: add a
download button next to the rounds/passes configuration in SetupRounds.svelte, add the required
i18n strings, and add an e2e test proving the full download flow works, including offline.

Purpose: Completes SHEET-01 (UI entry point) and SHEET-07 (offline verification) — the only two
requirements not fully covered by Plan 01's pure-function module.

Output: A working "Schießformular (PDF) drucken" button in SetupRounds.svelte, new
`strings.scoresheetExport` i18n section, and `e2e/scoresheetExport.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-blank-scoresheet-pdf/07-PATTERNS.md
</context>

<interfaces>
From src/lib/utils/scoresheetExport.ts (Plan 01 output — assume it exists with these exports):
```typescript
export function scoresheetPdfFilename(date?: Date): string;
export async function buildScoresheetPdfDoc(roundsConfig: RoundConfig, settings: Pick<SettingsRecord, 'title' | 'logoLeftBlob' | 'logoRightBlob'> | undefined): Promise<jsPDF>;
export async function generateScoresheetPdf(roundsConfig: RoundConfig, settings: Pick<SettingsRecord, 'title' | 'logoLeftBlob' | 'logoRightBlob'> | undefined): Promise<Blob>;
```

From src/lib/views/Results.svelte (download-trigger + error-handling pattern to replicate exactly):
```typescript
async function handleExport() {
  errorFeedback = '';
  try {
    const settings = (await db.settings.get(1)) ?? { id: 1 as const };
    const blob = await generateResultsPdf(rankings, classesWithResults, settings, includeIncomplete);
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = resultsPdfFilename();
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
  } catch {
    errorFeedback = strings.resultsPdf.exportError;
  }
}
```

From src/lib/db/schema.ts:
```typescript
rounds!: Table<RoundConfig, number>;
settings!: Table<SettingsRecord, number>;
```
`db.rounds.get(1)` and `db.settings.get(1)` are the singleton-row read patterns already used
elsewhere in the codebase (see `existingConfigQuery` in SetupRounds.svelte's current source).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add scoresheetExport i18n strings and wire the download button into SetupRounds.svelte</name>
  <files>src/lib/views/SetupRounds.svelte, src/lib/i18n/strings.de.ts</files>
  <read_first>
    src/lib/views/SetupRounds.svelte
    src/lib/views/Results.svelte
    src/lib/i18n/strings.de.ts
    src/lib/utils/scoresheetExport.ts
    .planning/phases/07-blank-scoresheet-pdf/07-PATTERNS.md
  </read_first>
  <action>
    In `src/lib/i18n/strings.de.ts`, append a new top-level key `scoresheetExport` (after the
    existing `certificateExport` block, before the file's closing brace) with exactly two
    string properties: `downloadButton: 'Schießformular (PDF) drucken'` and
    `exportError: 'Schießformular konnte nicht generiert werden'`.

    In `src/lib/views/SetupRounds.svelte`:
    - Add imports: `import { FileDown } from '@lucide/svelte';` and
      `import { generateScoresheetPdf, scoresheetPdfFilename } from '../utils/scoresheetExport';`
    - Add a `let errorFeedback = $state('');` local state variable (mirrors Results.svelte's
      pattern; SetupRounds.svelte currently has no error display, this introduces it).
    - Add an async `handleScoresheetExport()` function using the existing `existingConfig`
      derived value from the component's own `liveQuery(() => db.rounds.get(1))` (already
      present in the file — do not create a second query). If `existingConfig` is falsy, set
      `errorFeedback` to a short inline message and return early (no db write needed here since
      SetupRounds.svelte already writes db.rounds via `save()` on every field change, so a valid
      row should exist by the time this button is enabled).
    - Fetch settings via `(await db.settings.get(1)) ?? { id: 1 as const }`, call
      `generateScoresheetPdf(existingConfig, settings)`, then perform the exact
      DOM-append/click/remove/revoke download-trigger sequence from Results.svelte's
      `handleExport` (using `scoresheetPdfFilename()` for `a.download`), wrapped in try/catch
      setting `errorFeedback = strings.scoresheetExport.exportError` on failure.
    - Add a `<button>` element after the existing summary `<p>` element (the resolvedConfig
      summary line, last element in the template) with: `type="button"`,
      `onclick={handleScoresheetExport}`, `disabled={!existingConfig}`, a `<FileDown size={20} />`
      icon, and text `{strings.scoresheetExport.downloadButton}` — use the same Tailwind class
      string already used for buttons elsewhere in this codebase (teal-500/teal-600 background,
      min-h-[44px], rounded-lg, disabled:opacity-50, dark: variants) matching Results.svelte's
      export button classes verbatim for visual consistency.
    - Render `{#if errorFeedback}` a paragraph showing `errorFeedback` beneath the button, styled
      consistent with any existing error text pattern in this codebase (check
      settingsForm-related components for the error-text class convention if unsure).
  </action>
  <verify>
    <automated>npx svelte-check --workspace . 2>&1 | grep -i "SetupRounds" || echo "no errors in SetupRounds.svelte"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/i18n/strings.de.ts` contains `scoresheetExport: {` with `downloadButton` and `exportError` keys
    - `src/lib/views/SetupRounds.svelte` imports `generateScoresheetPdf` and `scoresheetPdfFilename` from `'../utils/scoresheetExport'`
    - `src/lib/views/SetupRounds.svelte` contains a `<button>` with `onclick={handleScoresheetExport}`
    - `npx svelte-check --workspace .` reports zero new errors attributable to SetupRounds.svelte
  </acceptance_criteria>
  <done>Button renders in SetupRounds.svelte, wired to generateScoresheetPdf with correct filename and error handling; svelte-check passes for this file.</done>
</task>

<task type="auto">
  <name>Task 2: Add e2e coverage for the scoresheet download flow, including offline mode</name>
  <files>e2e/scoresheetExport.spec.ts</files>
  <read_first>
    e2e/pdfExport.spec.ts
    e2e/setupLayout.spec.ts
    src/lib/views/SetupRounds.svelte
  </read_first>
  <action>
    Create `e2e/scoresheetExport.spec.ts` following the structure of `e2e/pdfExport.spec.ts`
    (Playwright, `test.describe`/`test` blocks, fresh browser context per test). Write a helper
    `setUpTournament(page)` that navigates to `/`, reaches the Einrichtung (Setup) view (mirror
    the navigation steps used in `e2e/setupLayout.spec.ts` or `e2e/pdfExport.spec.ts` — whichever
    already demonstrates reaching Setup/Einrichtung), and leaves the rounds/passes config at its
    default WA preset (no custom config needed since SHEET-01/07 just need *any* valid config to
    exist).

    Test 1 — `'clicking "Schießformular (PDF) drucken" downloads a correctly-named PDF'`: call
    the helper, then `page.getByRole('button', { name: 'Schießformular (PDF) drucken' }).click()`
    inside a `page.waitForEvent('download')` pattern, then assert
    `download.suggestedFilename()` matches `/^Schießformular_\d{4}-\d{2}-\d{2}\.pdf$/`.

    Test 2 — `'scoresheet export succeeds with zero network connectivity (offline)'`: same setup,
    then `await context.setOffline(true)` before clicking the button, assert the same filename
    pattern on the resulting download, then `await context.setOffline(false)` afterward (SHEET-07).

    Test 3 — `'scoresheet grid reflects a custom rounds/passes/arrows configuration'`: navigate to
    Einrichtung, switch to the "Benutzerdefiniert" (custom) radio in the rounds section, fill in
    Runden=2, Passen pro Runde=5, Pfeile pro Passe=4 (use `getByLabel` matching the exact label
    text strings already used in SetupRounds.svelte's template: `strings.setup.roundsCountLabel`,
    `strings.setup.passesPerRoundLabel`, `strings.setup.arrowsPerPassLabel` — read their actual
    string values from `src/lib/i18n/strings.de.ts` if not already known from context), blur the
    last field to trigger `save()`, then click the download button and assert on
    `download.suggestedFilename()` matching the same filename pattern (SHEET-02 — this test
    proves the button is reachable and functional after a custom config change; exact grid
    pixel content is not assertable via Playwright's download event, so filename + no-crash is
    the achievable e2e proof, with grid dimension coverage already handled by Plan 01's Vitest
    unit tests).
  </action>
  <verify>
    <automated>npx playwright test e2e/scoresheetExport.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/scoresheetExport.spec.ts` exists with exactly 3 tests as described
    - `npx playwright test e2e/scoresheetExport.spec.ts` passes all 3 tests
    - Test 2 explicitly calls `context.setOffline(true)` and asserts a successful download while offline
  </acceptance_criteria>
  <done>All 3 e2e tests pass, confirming the button downloads a correctly-named PDF both online and fully offline, and remains functional after a custom rounds/passes config change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → db.rounds/db.settings read → PDF generation → browser download | Entirely local; no network calls involved in this flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | Downloaded PDF file | accept | PDF contains only locally-entered tournament config (title, logos) and blank fields — no sensitive data, single-device offline app with no multi-user access control model |
| T-07-04 | Denial of Service | handleScoresheetExport with missing db.rounds row | mitigate | Function checks `existingConfig` truthiness before calling generateScoresheetPdf and shows errorFeedback instead of throwing; button also disabled via `disabled={!existingConfig}` |
</threat_model>

<verification>
Run `npx svelte-check --workspace .` — no new errors in SetupRounds.svelte.
Run `npx playwright test e2e/scoresheetExport.spec.ts` — all 3 tests pass, including the
offline-mode test.
</verification>

<success_criteria>
Trainer can navigate to Einrichtung, click "Schießformular (PDF) drucken", and receive a
correctly-named PDF download, both online and fully offline, for both preset and custom
rounds/passes configurations.
</success_criteria>

<output>
Create `.planning/phases/07-blank-scoresheet-pdf/07-02-SUMMARY.md` when done
</output>
