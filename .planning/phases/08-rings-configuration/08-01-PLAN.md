---
phase: 08-rings-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/fixtures/waPresets.ts
  - src/lib/i18n/strings.de.ts
autonomous: true
requirements: [TARGET-01, TARGET-03]

must_haves:
  truths:
    - "RoundConfig records without a rings field are treated as 10-ring by every reader that applies the `?? 10` fallback (no migration write)"
    - "Exactly three Vorlagen presets exist: WA 10 Passen à 3 Pfeile (rings=10), DFBV 6 Runden à 5 Pfeile (rings=5), WA 70 (rings=10)"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "RoundConfig.rings?: 10 | 5 field, RoundConfig.distance made optional"
      contains: "rings?: 10 | 5"
    - path: "src/lib/fixtures/waPresets.ts"
      provides: "3 new WA_PRESETS entries carrying fixed rings values"
      contains: "dfbv-6x5"
    - path: "src/lib/i18n/strings.de.ts"
      provides: "renamed/new preset + rings-radio label strings"
      contains: "customRingsLabel"
  key_links:
    - from: "src/lib/fixtures/waPresets.ts"
      to: "src/lib/db/schema.ts RoundConfig"
      via: "preset.rings assigned into RoundConfig.rings at consumption time (Plan 02)"
      pattern: "rings:\\s*10|rings:\\s*5"
---

<objective>
Add the `rings` field to the `RoundConfig` data model and replace the existing 3 WA
presets with the 3 locked v1.3 presets ("WA 10 Passen à 3 Pfeile", "DFBV 6 Runden à 5
Pfeile", "WA 70"), each carrying a fixed `rings` value. Update the German UI strings that
Plan 02's `SetupRounds.svelte` rewiring will consume.

Purpose: Establish the data-model and reference-data foundation (TARGET-01, TARGET-03)
before any UI code touches it, so Plan 02 implements against stable contracts.
Output: Updated `schema.ts` type, replaced `waPresets.ts` fixture, updated
`strings.de.ts` label keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-rings-configuration/08-RESEARCH.md
@src/lib/db/schema.ts
@src/lib/fixtures/waPresets.ts
@src/lib/i18n/strings.de.ts
</context>

<interfaces>
Current `RoundConfig` (src/lib/db/schema.ts:20-27):
```typescript
export interface RoundConfig {
  id?: number;
  arrowsPerPasse: number;
  passesPerRound: number;
  numberOfRounds: number;
  distance: string;
  presetId?: string;
}
```

Current `WA_PRESETS` (src/lib/fixtures/waPresets.ts) — array of 3 objects with
`id, name, arrowsPerPasse, passesPerRound, numberOfRounds, distance, totalArrows`,
declared `as const`.

Current relevant `strings.de.ts` keys (lines 67-75, `setup` section):
```
waPresetsLabel: 'WA-Vorlagen',
customLabel: 'Benutzerdefiniert',
wa18m: 'WA 18m — 10 Passen à 3 Pfeile (30 gesamt)',
wa25m: 'WA 25m — 10 Passen à 3 Pfeile (30 gesamt)',
wa70m: 'WA 70m — 6 Passen à 6 Pfeile (36 gesamt)',
roundsCountLabel: 'Runden',
passesPerRoundLabel: 'Passen pro Runde',
arrowsPerPassLabel: 'Pfeile pro Passe',
customDistanceLabel: 'Entfernung',
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add rings field to RoundConfig, loosen distance</name>
  <files>src/lib/db/schema.ts</files>
  <action>
  In the `RoundConfig` interface (schema.ts:20-27), add `rings?: 10 | 5;` as a new
  optional field (literal union type per D-locked decision and research Pitfall 3 — not
  `number`). Change `distance: string;` to `distance?: string;` — it is no longer
  required now that custom mode (Plan 02) replaces it with the Auflagen radio, and
  research confirms this is the only `RoundConfig.distance` consumer left after Plan 02;
  do NOT touch `ClassRecord.distance` (schema.ts:7-13), which is an unrelated field for
  Bogen-/Altersklassen and already optional. Do NOT add a Dexie `.version()` bump —
  per TARGET-01 and research, `rings` is read with a `?? 10` fallback at every
  consumption site, no migration required, matching the existing `presetId?: string`
  precedent.
  </action>
  <verify>
    <automated>cd /home/code/Archery/InstantBogenturnier && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "vite.config" || true</automated>
  </verify>
  <done>RoundConfig has `rings?: 10 | 5` and `distance?: string`; no `.version()` block changed in schema.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Replace WA_PRESETS and update strings.de.ts labels</name>
  <files>src/lib/fixtures/waPresets.ts, src/lib/i18n/strings.de.ts</files>
  <action>
  In `waPresets.ts`, replace the 3 existing preset objects with exactly these 3 (per
  locked decision, all `as const`, keep the `totalArrows` derived-reference-data
  convention and the module comment style already present):
  - `{ id: 'wa-10x3', name: 'WA 10 Passen à 3 Pfeile', arrowsPerPasse: 3, passesPerRound: 10, numberOfRounds: 1, rings: 10, totalArrows: 30 }`
  - `{ id: 'dfbv-6x5', name: 'DFBV 6 Runden à 5 Pfeile', arrowsPerPasse: 5, passesPerRound: 1, numberOfRounds: 6, rings: 5, totalArrows: 30 }`
  - `{ id: 'wa-70', name: 'WA 70', arrowsPerPasse: 6, passesPerRound: 6, numberOfRounds: 1, rings: 10, totalArrows: 36 }`
  Drop the `distance` field entirely from these preset objects (distance is no longer
  the differentiator; `RoundConfig.distance` is now optional per Task 1). This
  consolidates the old `wa-18m`/`wa-25m` presets into the single `wa-10x3` preset per
  the locked decision.

  In `strings.de.ts` (`setup` section, current lines 67-75), make these exact changes:
  - `waPresetsLabel: 'WA-Vorlagen'` → `waPresetsLabel: 'Vorlagen'` (TARGET-02; the
    `customLabel` value "Benutzerdefiniert" is already correct and unchanged).
  - Remove `wa18m`, `wa25m`, `wa70m` keys entirely.
  - Add three new preset-label keys whose values are the exact locked preset names:
    `presetWa10x3: 'WA 10 Passen à 3 Pfeile'`, `presetDfbv6x5: 'DFBV 6 Runden à 5 Pfeile'`,
    `presetWa70: 'WA 70'`.
  - Remove `customDistanceLabel: 'Entfernung'`.
  - Add `customRingsLabel: 'Auflagen'`, `rings10Label: '10 Ringe'`, `rings5Label: '5 Ringe'`
    (new radio-group heading + two option labels for Plan 02's custom-mode Auflagen radio).
  - Leave `roundsCountLabel`, `passesPerRoundLabel`, `arrowsPerPassLabel` unchanged.
  </action>
  <verify>
    <automated>cd /home/code/Archery/InstantBogenturnier && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "vite.config" || true</automated>
  </verify>
  <done>WA_PRESETS exports exactly 3 objects with the ids/names/shapes above, each carrying `rings`; strings.de.ts's `setup` section has the renamed/added/removed keys exactly as specified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan only changes a TypeScript type definition and static reference data/string literals — no untrusted input, no new I/O, no user-controlled data path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `RoundConfig.rings` | accept | Value is radio-constrained to `10 | 5` at every write site (Plan 02); no free-text/user-supplied path exists in this plan's scope. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` shows no new errors introduced by this plan (the pre-existing `vite.config.ts` module-resolution error is known tech debt, unrelated — filter it out).
- `grep -n "rings" src/lib/db/schema.ts` shows the new field.
- `grep -c "id: '" src/lib/fixtures/waPresets.ts` equals 3.
</verification>

<success_criteria>
- `RoundConfig` type includes `rings?: 10 | 5` with no Dexie version bump.
- `WA_PRESETS` contains exactly the 3 locked presets with correct shapes and `rings` values.
- `strings.de.ts` has the renamed/new/removed keys exactly as specified, ready for Plan 02 to consume.
</success_criteria>

<output>
Create `.planning/phases/08-rings-configuration/08-01-SUMMARY.md` when done
</output>
