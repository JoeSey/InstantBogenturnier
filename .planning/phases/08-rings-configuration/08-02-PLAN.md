---
phase: 08-rings-configuration
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/lib/views/SetupRounds.svelte
  - src/lib/views/SetupRounds.test.ts
  - src/lib/components/PresetSave.svelte
  - src/lib/components/PresetSave.test.ts
autonomous: true
requirements: [TARGET-02, TARGET-03, TARGET-04]

must_haves:
  truths:
    - "Trainer can pick one of the 3 Vorlagen presets, each silently applying its correct Auflagen (10 or 5) with no separate rings control shown in preset mode"
    - "Trainer in Benutzerdefiniert mode explicitly chooses Auflagen 10 or 5 via a radio control, replacing the old free-text distance field"
    - "The top-level radio group reads 'Vorlagen/Benutzerdefiniert'"
    - "Saving a named preset persists the active rings value (does not silently drop it)"
  artifacts:
    - path: "src/lib/views/SetupRounds.svelte"
      provides: "custom-mode Auflagen radio, rings-aware resolvedConfig/hydration/validation, updated preset label map"
      contains: "customRings"
    - path: "src/lib/components/PresetSave.svelte"
      provides: "rings persisted into saved PresetRecord.roundsConfig"
      contains: "rings: roundsRecord.rings"
  key_links:
    - from: "src/lib/views/SetupRounds.svelte resolvedConfig"
      to: "db.rounds.put"
      via: "save() persists resolvedConfig including rings"
      pattern: "rings:\\s*(preset\\.rings|customRings)"
    - from: "src/lib/components/PresetSave.svelte performSave"
      to: "db.presets.put"
      via: "roundsConfig object includes rings field read from db.rounds"
      pattern: "rings:\\s*roundsRecord\\.rings"
---

<objective>
Rewire `SetupRounds.svelte`'s custom mode to use an explicit Auflagen (10/5) radio in
place of the free-text distance field, wire preset-derived `rings` through
`resolvedConfig`/hydration/validation, and fix `PresetSave.svelte` so saved presets no
longer silently drop the `rings` setting (research Pitfall 1). Update both components'
existing test suites to match.

Purpose: Deliver the trainer-facing Auflagen configuration UI (TARGET-02, TARGET-03,
TARGET-04) on top of Plan 01's data-model/fixture foundation.
Output: Working Vorlagen/Benutzerdefiniert rings selection in Einrichtung, with rings
surviving the save-preset round trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-rings-configuration/08-RESEARCH.md
@.planning/phases/08-rings-configuration/08-01-SUMMARY.md
@src/lib/views/SetupRounds.svelte
@src/lib/views/SetupRounds.test.ts
@src/lib/components/PresetSave.svelte
@src/lib/components/PresetSave.test.ts
</context>

<interfaces>
After Plan 01, `RoundConfig` (src/lib/db/schema.ts) is:
```typescript
export interface RoundConfig {
  id?: number;
  arrowsPerPasse: number;
  passesPerRound: number;
  numberOfRounds: number;
  distance?: string;
  presetId?: string;
  rings?: 10 | 5;
}
```

`WA_PRESETS` (src/lib/fixtures/waPresets.ts) after Plan 01 — 3 entries, ids
`wa-10x3` / `dfbv-6x5` / `wa-70`, each with `rings: 10 | 5`, no `distance` field.

`strings.de.ts` `setup` section after Plan 01 includes: `waPresetsLabel: 'Vorlagen'`,
`customLabel: 'Benutzerdefiniert'`, `presetWa10x3`, `presetDfbv6x5`, `presetWa70`,
`customRingsLabel: 'Auflagen'`, `rings10Label: '10 Ringe'`, `rings5Label: '5 Ringe'`,
`roundsCountLabel`, `passesPerRoundLabel`, `arrowsPerPassLabel` (unchanged).
`customDistanceLabel`, `wa18m`, `wa25m`, `wa70m` no longer exist.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire SetupRounds.svelte for rings + update its tests</name>
  <files>src/lib/views/SetupRounds.svelte, src/lib/views/SetupRounds.test.ts</files>
  <behavior>
    - Selecting the "WA 10 Passen à 3 Pfeile" preset persists `db.rounds` with
      `arrowsPerPasse: 3, passesPerRound: 10, numberOfRounds: 1, rings: 10, presetId: 'wa-10x3'`.
    - Selecting the "DFBV 6 Runden à 5 Pfeile" preset persists
      `arrowsPerPasse: 5, passesPerRound: 1, numberOfRounds: 6, rings: 5, presetId: 'dfbv-6x5'`.
    - In Benutzerdefiniert mode, choosing the "5 Ringe" radio (with other custom fields
      at their defaults) persists a `db.rounds` record with `rings: 5, presetId: undefined`.
    - Rehydrating from an existing custom `db.rounds` record with `rings: 5` checks the
      "5 Ringe" radio on mount.
    - Rehydrating from an existing custom `db.rounds` record with no `rings` field
      defaults the "10 Ringe" radio checked (TARGET-01 default-to-10 fallback).
    - `isFinalized: true` disables the new Auflagen radio inputs, matching the existing
      disabled-state test for all other controls.
  </behavior>
  <action>
  In `SetupRounds.svelte`:
  1. Update `presetLabels` (lines 14-18) to map the 3 new ids to the 3 new string keys:
     `'wa-10x3': strings.setup.presetWa10x3`, `'dfbv-6x5': strings.setup.presetDfbv6x5`,
     `'wa-70': strings.setup.presetWa70`.
  2. Replace `let customDistance = $state('18m');` (line 26) with
     `let customRings = $state<10 | 5>(10);`.
  3. In the hydration `$effect` (lines 35-49), custom branch: replace
     `customDistance = cfg.distance;` with `customRings = cfg.rings ?? 10;`.
  4. In `resolvedConfig` (lines 73-91): preset branch — drop `distance: preset.distance`,
     add `rings: preset.rings as 10 | 5`. Custom branch — drop
     `distance: customDistance`, add `rings: customRings`.
  5. In `isValidResolvedConfig` (lines 97-109), add
     `(config.rings === 10 || config.rings === 5) &&` to the conjunction — matches this
     function's existing defensive per-field style.
  6. In the custom-mode block (lines 200-250), replace the `customDistanceLabel` text
     input (lines 240-249) with a two-radio group bound to `customRings`, following the
     exact same pattern, `disabled={isFinalized}`, and Tailwind classes as the top-level
     mode selector radios (lines 144-177): a heading/label using
     `strings.setup.customRingsLabel`, one radio with `value="10"` labeled
     `strings.setup.rings10Label` (`onchange={() => { customRings = 10; save(); }}`), one
     radio with `value="5"` labeled `strings.setup.rings5Label`
     (`onchange={() => { customRings = 5; save(); }}`), both `name="custom-rings"`,
     `checked={customRings === 10}` / `checked={customRings === 5}` respectively.
  7. Update the summary line (line 253-255): replace `{resolvedConfig.distance}` with
     `{resolvedConfig.rings} Ringe` (keep the "Passen"/"Pfeile" text and structure
     otherwise unchanged), e.g.
     `{resolvedConfig.passesPerRound} Passen, {resolvedConfig.arrowsPerPasse} Pfeile, {resolvedConfig.rings} Ringe`.

  In `SetupRounds.test.ts`, rewrite all 5 existing tests to match the new preset
  ids/labels/shapes and the new custom-mode Auflagen radio (no more `customDistanceLabel`
  input, no `distance` field in any expected `db.rounds` snapshot — use `rings` instead):
  update the preset-selection test to select `strings.setup.presetDfbv6x5` (or another
  non-default preset, mirroring the existing "select a non-default preset to trigger a
  genuine onchange" pattern) and assert the DFBV shape/summary text
  ("1 Passen, 5 Pfeile, 5 Ringe"); update the custom-configuration test to interact with
  the new Auflagen radio instead of the removed distance input and assert `rings` in the
  persisted record; update both rehydration tests to seed `db.rounds` with `rings`
  (5 for one, omitted for the other to prove the `?? 10` default) and assert the correct
  radio is checked; update the `isFinalized` disabled-state test to also assert the two
  new Auflagen radios are disabled (via `strings.setup.rings10Label`/`rings5Label`) after
  clicking into custom mode.
  </action>
  <verify>
    <automated>cd /home/code/Archery/InstantBogenturnier && npx vitest run src/lib/views/SetupRounds.test.ts</automated>
  </verify>
  <done>All SetupRounds.test.ts tests pass; SetupRounds.svelte has no remaining reference to `customDistance`, `customDistanceLabel`, or `resolvedConfig.distance`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix PresetSave.svelte rings pass-through + update its tests</name>
  <files>src/lib/components/PresetSave.svelte, src/lib/components/PresetSave.test.ts</files>
  <behavior>
    - Saving a new preset while `db.rounds` has `rings: 5` persists
      `PresetRecord.roundsConfig.rings === 5`.
    - Saving a new preset while `db.rounds` has no `rings` field (or no `db.rounds`
      record at all) persists `PresetRecord.roundsConfig.rings === 10` (default-to-10
      fallback, consistent with TARGET-01).
  </behavior>
  <action>
  In `PresetSave.svelte`'s `performSave` function (lines 19-35), add
  `rings: roundsRecord.rings ?? 10` to the `roundsConfig` object literal built when
  `roundsRecord` exists (alongside the existing explicit field list —
  `arrowsPerPasse`/`passesPerRound`/`numberOfRounds`/`distance`/`presetId`), and add
  `rings: 10` to the no-`roundsRecord` fallback object literal (line 34) — this closes
  research Pitfall 1 (trainer saves a 5-ring config, reloads it later, silently becomes
  10-ring). Leave the existing `distance: roundsRecord.distance` line as-is (harmless
  now that `distance` is optional on `RoundConfig`).

  In `PresetSave.test.ts`, add a new test: seed `db.rounds` with a `rings: 5` record
  (plus a matching custom `arrowsPerPasse`/`passesPerRound`/`numberOfRounds`), render
  `PresetSave`, save a new preset by name, and assert
  `(await db.presets.where('name').equals(name).first())?.roundsConfig.rings === 5`.
  </action>
  <verify>
    <automated>cd /home/code/Archery/InstantBogenturnier && npx vitest run src/lib/components/PresetSave.test.ts</automated>
  </verify>
  <done>PresetSave.test.ts passes including the new rings-persistence assertion; `performSave` explicitly includes `rings` in both the `roundsRecord`-present and fallback branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Pure client-side form UI wiring an already-validated, radio-constrained field into local IndexedDB. No network, no untrusted external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | `SetupRounds.svelte` custom Auflagen radio | accept | Radio-constrained to `10`/`5`; `isValidResolvedConfig` also gates the write, consistent with existing field validation style. |
| T-08-03 | Information Disclosure / Repudiation | `PresetSave.svelte` rings pass-through | accept | Local-only IndexedDB write, single-device/single-user app, no cross-boundary data flow. |
</threat_model>

<verification>
- `npx vitest run src/lib/views/SetupRounds.test.ts src/lib/components/PresetSave.test.ts` — all pass.
- `grep -n "customDistance" src/lib/views/SetupRounds.svelte` returns no matches.
- `grep -n "rings: roundsRecord.rings" src/lib/components/PresetSave.svelte` returns a match.
</verification>

<success_criteria>
- Trainer can select any of the 3 Vorlagen presets and see the correct rings applied with no separate control shown.
- Trainer can explicitly choose Auflagen 10 or 5 in Benutzerdefiniert mode via radio, not free text.
- Top-level radio group reads "Vorlagen/Benutzerdefiniert".
- Saved presets round-trip the `rings` value correctly (no silent drop).
- All updated/existing tests in both files pass.
</success_criteria>

<output>
Create `.planning/phases/08-rings-configuration/08-02-SUMMARY.md` when done
</output>
