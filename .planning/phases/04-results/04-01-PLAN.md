---
phase: 04-results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/ranking.ts
  - src/lib/utils/ranking.test.ts
  - src/lib/i18n/strings.de.ts
  - src/lib/components/ResultsTable.svelte
  - src/lib/components/ResultsTable.test.ts
  - src/lib/components/ClassSelector.svelte
  - src/lib/views/Results.svelte
  - src/lib/views/Results.test.ts
  - src/App.svelte
  - e2e/results.spec.ts
autonomous: true
requirements: [RES-01, RES-02, RES-03, RES-04]

must_haves:
  truths:
    - "Trainer can view a per-class ranked results list showing rank, name, and total score, sorted descending by total"
    - "Tied totals share the same rank and the next distinct rank skips accordingly (1-2-2-4, not 1-2-2-3)"
    - "On a phone-width screen (<768px), only one class's results are visible at a time, chosen via a dropdown"
    - "On tablet/desktop screens (>=768px), multiple classes render simultaneously in a responsive 1/2/3-column grid"
    - "A shooter who hasn't finished every configured arrow shows a visible in-progress marker next to their total"
    - "Classes with zero registered shooters do not appear in the dropdown or grid at all"
  artifacts:
    - path: "src/lib/utils/ranking.ts"
      provides: "computeShooterSum, isShooterComplete, assignRanks, computeClassRankings pure functions + RankedRow type"
      exports: ["RankedRow", "computeShooterSum", "isShooterComplete", "computeClassRankings"]
    - path: "src/lib/components/ResultsTable.svelte"
      provides: "Fixed-order per-class ranked table (Rang/Name/Schießplatz/Gesamt) with podium badges and in-progress marker"
    - path: "src/lib/components/ClassSelector.svelte"
      provides: "Phone-only native <select> class switcher"
    - path: "src/lib/views/Results.svelte"
      provides: "Main Results view: liveQuery wiring, dual-render phone/grid layout, empty state"
  key_links:
    - from: "src/lib/views/Results.svelte"
      to: "src/lib/utils/ranking.ts"
      via: "computeClassRankings(shooters, classes, allScores, roundsConfig) call feeding $derived rankings"
      pattern: "computeClassRankings\\("
    - from: "src/App.svelte"
      to: "src/lib/views/Results.svelte"
      via: "views map entry results: Results (replacing ResultsPlaceholder)"
      pattern: "results:\\s*Results"
    - from: "src/lib/components/ResultsTable.svelte"
      to: "src/lib/utils/ranking.ts"
      via: "import type { RankedRow } from '../utils/ranking'"
      pattern: "RankedRow"
---

<objective>
Build the live, ranked, responsive Results view: a pure tournament-wide ranking function (RES-01/RES-02), and the Results.svelte view that renders it as a phone dropdown-plus-table or a tablet/desktop responsive grid (RES-03/RES-04), replacing `ResultsPlaceholder.svelte` in the nav.

Purpose: This is the phase's primary vertical slice — after this plan, a trainer can navigate to "Ergebnisse" and see accurate, correctly-ranked, per-class standings on any device, live-updating as scores are entered elsewhere (D-01).
Output: `src/lib/utils/ranking.ts`, `src/lib/components/ResultsTable.svelte`, `src/lib/components/ClassSelector.svelte`, `src/lib/views/Results.svelte`, wired into `src/App.svelte`, plus the full `results` i18n string section (including reset/guard strings consumed by later plans in this phase).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results/04-CONTEXT.md
@.planning/phases/04-results/04-UI-SPEC.md
</context>

<interfaces>
<!-- RankedRow is the contract every downstream consumer in this plan (ResultsTable.svelte,
     Results.svelte) must use directly — do not re-derive a parallel shape. -->

From src/lib/db/schema.ts (existing, read-only for this plan):
```typescript
export interface ClassRecord { id?: number; name: string; ageGroup?: string; bowType?: string; distance?: string; }
export interface ShooterRecord { id?: number; name: string; classId: number; lineAssignment?: number | null; flight?: 'A/B' | 'C/D' | null; }
export interface ScoreRecord { shooterId: number; roundIndex: number; passeIndex: number; arrowIndex: number; value: ScoreValue; finalized: boolean; }
export interface RoundConfig { id?: number; arrowsPerPasse: number; passesPerRound: number; numberOfRounds: number; distance: string; presetId?: string; }
```

From src/lib/utils/scoreCompletion.ts (existing, reuse — do not reimplement):
```typescript
export function arrowScoreValue(value: ScoreValue): number
export function areAllScoresEntered(shooterIds: number[], numberOfRounds: number, passesPerRound: number, arrowsPerPasse: number, scores: ScoreRecord[]): boolean
```

To be created in Task 1 (src/lib/utils/ranking.ts) — the contract Task 2/3 build against:
```typescript
export interface RankedRow {
  shooterId: number;
  name: string;
  line: number | null;
  sum: number;
  rank: number;
  isComplete: boolean;
}
export function computeShooterSum(shooterId: number, scores: ScoreRecord[]): number
export function isShooterComplete(shooterId: number, roundsConfig: RoundConfig, scores: ScoreRecord[]): boolean
export function computeClassRankings(shooters: ShooterRecord[], classes: ClassRecord[], scores: ScoreRecord[], roundsConfig: RoundConfig | undefined): Map<number, RankedRow[]>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure tournament-wide ranking function (RES-01, RES-02, D-02)</name>
  <files>src/lib/utils/ranking.ts, src/lib/utils/ranking.test.ts</files>
  <read_first>
    src/lib/utils/scoreCompletion.ts (full file — exact banner-comment/plain-function style to copy; reuse arrowScoreValue and areAllScoresEntered, do not reimplement M/X mapping per Pitfall 2)
    src/lib/utils/scoreCompletion.test.ts (full file — describe/it structure and record() fixture-builder convention to mirror)
    src/lib/db/schema.ts (ClassRecord, ShooterRecord, ScoreRecord, RoundConfig shapes)
  </read_first>
  <behavior>
    - computeShooterSum(shooterId, scores): filters scores by shooterId only (NOT scoped to a round/passe), reduces with arrowScoreValue. A 2-round tournament's sum must reflect BOTH rounds (Pitfall 2 regression fixture).
    - isShooterComplete(shooterId, roundsConfig, scores): delegates to areAllScoresEntered([shooterId], roundsConfig.numberOfRounds, roundsConfig.passesPerRound, roundsConfig.arrowsPerPasse, scores) — does not reimplement cell-completeness checking.
    - assignRanks: standard competition ranking on a sum-descending-sorted array. A 4-shooter fixture with a 2-way tie for the 2nd-highest sum MUST assert ranks [1, 2, 2, 4] (never [1, 2, 2, 3] — Pitfall 1).
    - computeClassRankings(shooters, classes, scores, roundsConfig): returns an empty Map immediately if roundsConfig is undefined. For each class, filters shooters by classId; classes with 0 matching shooters are OMITTED from the returned Map entirely (not present as an empty array — D-04/edge case). Sorts each class's shooters descending by sum, alphabetical-by-name as the row-order tiebreak (rank number identical either way). Every shooter appears regardless of isComplete (D-02) — there is no separate "in-progress" bucket.
  </behavior>
  <action>
    Create src/lib/utils/ranking.ts following scoreCompletion.ts's exact convention: plain exported functions, no framework imports, a banner comment above the file referencing D-01/D-02/RES-01/RES-02. Import { arrowScoreValue, areAllScoresEntered } from './scoreCompletion' — do not duplicate the M/X-to-number mapping. Export the RankedRow interface exactly as specified in the Interfaces block above. Implement assignRanks as an internal (non-exported, or exported for direct testing — your choice, but ranking.test.ts should be able to reach it either via computeClassRankings's output ranks or a direct export) helper using "rank = 1-based index of the first occurrence of this sum in the sorted array" (i + 1 on a sum change, not an incrementing counter). Create ranking.test.ts mirroring scoreCompletion.test.ts's describe-per-function/it-per-behavior structure with a small shooter()/record() fixture builder.
  </action>
  <acceptance_criteria>
    - ranking.test.ts contains a 4-shooter, 2-way-tie-for-rank-2 fixture asserting ranks equal exactly [1, 2, 2, 4]
    - ranking.test.ts contains a 2-round cross-round-sum fixture asserting the total reflects arrows from both rounds (not just round 0)
    - ranking.test.ts contains an all-shooters-incomplete fixture asserting every row is still present with isComplete: false (not excluded from the ranked list)
    - ranking.test.ts asserts a class with 0 shooters is absent from computeClassRankings's returned Map (Map.has(classId) is false, not an empty array)
    - `grep -n "import" src/lib/utils/ranking.ts` shows an import of arrowScoreValue and/or areAllScoresEntered from './scoreCompletion' (no reimplementation of the M/X value mapping)
    - `npx vitest run src/lib/utils/ranking.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/utils/ranking.test.ts</automated>
  </verify>
  <done>ranking.ts exports RankedRow + computeShooterSum/isShooterComplete/computeClassRankings; all ranking.test.ts cases pass, including the mandatory tie and cross-round fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: ResultsTable + ClassSelector components, full `results` strings section</name>
  <files>src/lib/components/ResultsTable.svelte, src/lib/components/ResultsTable.test.ts, src/lib/components/ClassSelector.svelte, src/lib/i18n/strings.de.ts</files>
  <read_first>
    src/lib/components/ScoreTable.svelte (full file — opaque-table shell, `hidden md:table-cell` responsive column pattern, row.line ?? '—' fallback convention; ResultsTable must NOT include onsort/onclick sort handlers, unlike this analog)
    src/lib/components/RoundPasseSelector.svelte (full file — native <select> + label layout convention for ClassSelector)
    src/lib/i18n/strings.de.ts (current file — insertion point: append `results` as the final property before the closing `} as const;` at line 161, mirroring the banner-comment convention above `scoring:` at line 128-130)
    .planning/phases/04-results/04-UI-SPEC.md (Podium Accent Colors table, In-Progress Marker section, Results Table Structure section — exact color tokens and copy)
  </read_first>
  <action>
    Append the full `results` object to strings.de.ts verbatim, as the final property before `} as const;`: heading: 'Ergebnisse', columnRank: 'Rang', columnName: 'Name', columnLine: 'Schießplatz', columnTotal: 'Gesamt', classDropdownLabel: 'Klasse', inProgressLegend: '* Ergebnis noch nicht vollständig — Erfassung läuft.', inProgressAria: 'Ergebnis noch nicht vollständig', emptyHeading: 'Noch keine Ergebnisse', emptyBody: 'Registrieren Sie Schützen und erfassen Sie Punkte, um Ergebnisse zu sehen.', resetButton: 'Neues Turnier starten', resetConfirmTitle: 'Neues Turnier starten?', resetConfirmBody: 'Alle Schützen und Punkte werden gelöscht. Klassen, Schießplätze und Runden/Passen bleiben erhalten. Diese Aktion kann nicht rückgängig gemacht werden. Fortfahren?', resetConfirmYes: 'Ja, zurücksetzen', resetConfirmCancel: 'Abbrechen', resetSuccess: 'Turnier zurückgesetzt. Klassen, Schießplätze und Runden/Passen sind erhalten.', resetError: 'Zurücksetzen fehlgeschlagen: {error}', guardMessage: 'Turnier abgeschlossen — Zurücksetzen, um zu ändern.' (this section is the single append point for ALL Phase 4 strings, including the reset/guard strings consumed by Plans 02/03 of this phase — do not create a second `results`-like block later).

    Create src/lib/components/ResultsTable.svelte with a `<script module lang="ts">` block importing `type { RankedRow } from '../utils/ranking'` and accepting props `{ rows: RankedRow[] }` via `$props()`. Render an opaque table (bg-white/dark:bg-slate-800, border-slate-200/dark:border-slate-600, wrapped in overflow-x-auto) with exactly 4 fixed-order columns and NO onsort/onclick sort handlers: (1) Rang — for rank <= 3 render a rounded-full 24x24 badge (h-6 w-6, Label role 14px/600, aria-label={`Rang ${rank}`}) with rank-1 classes bg-amber-100 text-amber-800 dark:bg-amber-900/50 dark:text-amber-300, rank-2 classes bg-slate-200 text-slate-700 dark:bg-slate-600 dark:text-slate-200, rank-3 classes bg-orange-100 text-orange-800 dark:bg-orange-900/50 dark:text-orange-300; rank >= 4 renders plain text with the same aria-label; (2) Name; (3) Schießplatz — `hidden md:table-cell` on both th and td (row.line ?? '—'); (4) Gesamt — right-aligned, font-semibold, showing row.sum, and when !row.isComplete append a muted `*` (span class text-slate-400 dark:text-slate-500) immediately after the numeral with an adjacent visually-hidden span (class="sr-only") containing strings.results.inProgressAria. Below the table, when any row has isComplete === false, render one Label-role (14px) legend line with strings.results.inProgressLegend. Use strings.results.columnRank/columnName/columnLine/columnTotal for header text. Data cells: px-2 py-2 md:px-3 md:py-2; header cells: p-2 md:p-4 (deliberately NOT ScoreTable's px-1.5 py-1.5, per UI-SPEC's spacing exception).

    Create src/lib/components/ClassSelector.svelte accepting props `{ classes: ClassRecord[], selectedClassId: number | null, onchange: (id: number) => void }` (import `type { ClassRecord } from '../db/schema'`). Render a `<label><span>{strings.results.classDropdownLabel}</span><select onchange={(e) => onchange(Number((e.target as HTMLSelectElement).value))}>...</select></label>` following RoundPasseSelector.svelte's exact class/layout conventions, with `<option value={cls.id}>{cls.name}</option>` for each class in the (caller-pre-sorted) classes array.

    Create src/lib/components/ResultsTable.test.ts (new, @testing-library/svelte render-with-fixture-props pattern, no Dexie/liveQuery needed since this is a pure presentational component) asserting: all 4 column headers render with the exact German copy; a fixture with ranks 1/2/3/4 renders the correct podium badge Tailwind classes for ranks 1-3 and plain text for rank 4; a fixture row with isComplete: false renders the `*` marker plus the sr-only aria text; the legend line only appears when at least one row is incomplete; no onclick/button elements exist in the header row (fixed order, no sort).
  </action>
  <acceptance_criteria>
    - `grep -c "onsort\|onclick={() => onsort" src/lib/components/ResultsTable.svelte` returns 0
    - strings.de.ts's new `results` object contains all 18 keys listed in the action (verify via `grep -A20 "results: {" src/lib/i18n/strings.de.ts` showing guardMessage as the last key before the closing brace)
    - ResultsTable.test.ts asserts rank-1 badge has both `bg-amber-100` and `dark:bg-amber-900/50` in its class list
    - ResultsTable.test.ts asserts an isComplete:false row's Gesamt cell contains the literal `*` character plus an element with strings.results.inProgressAria text
    - `npx vitest run src/lib/components/ResultsTable.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/components/ResultsTable.test.ts</automated>
  </verify>
  <done>ResultsTable.svelte and ClassSelector.svelte exist, render correctly against fixture props, and the full `results` i18n section is present in strings.de.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Results.svelte view — liveQuery wiring, dual-render layout, empty state, nav wiring</name>
  <files>src/lib/views/Results.svelte, src/lib/views/Results.test.ts, src/App.svelte, e2e/results.spec.ts</files>
  <read_first>
    src/lib/views/ScoreEntry.svelte (lines 21-32 multi-liveQuery + `?? []` defaulting pattern; lines 256-263 heading/error-row wrapper shape to copy)
    src/lib/views/Registration.svelte (lines 96-176 — dual-render table/card-list idiom, the closest in-repo analog to the phone-dropdown-vs-grid split needed here)
    src/App.svelte (lines 1-29, 52-53 — dual-render `class="flex md:hidden"` / `class="hidden md:flex"` idiom, and the views map/import to modify)
    e2e/nav.spec.ts (lines 9-23 — exact `page.setViewportSize` + `toBeVisible`/`toBeHidden` breakpoint-assertion shape)
    src/lib/views/ResultsPlaceholder.svelte (current placeholder being replaced in the views map — do NOT delete this file, other historical placeholders remain unused in the tree per existing convention)
  </read_first>
  <action>
    Create src/lib/views/Results.svelte. Run four independent liveQuery's: `liveQuery(() => db.shooters.toArray())`, `liveQuery(() => db.classes.toArray())`, `liveQuery(() => db.rounds.get(1))`, `liveQuery(() => db.scores.toArray())`, each defaulted (`?? []` for shooters/classes/scores, bare for rounds). Compute `let rankings = $derived(computeClassRankings(shooters, classes, allScores, roundsConfig));` (import from '../utils/ranking'). Compute `let classesWithResults = $derived(classes.filter((c) => rankings.has(c.id!)).sort((a, b) => a.name.localeCompare(b.name)));` — this is the D-04 alphabetical, edge-case-safe (empty classes excluded per UI-SPEC) class list feeding BOTH the phone dropdown and the grid, replacing a naive `classes` reference. State `let selectedClassId = $state<number | null>(null);` with an `$effect` that sets it to `classesWithResults[0]?.id ?? null` whenever the current selection is null or no longer present in classesWithResults (handles first-load default AND the post-reset re-pick case in the next plan). Empty state condition: `classesWithResults.length === 0` — render a centered Body-role message using strings.results.emptyHeading/emptyBody, no table rendered. Otherwise render two always-in-DOM branches (never window.matchMedia/innerWidth): phone branch `<div class="md:hidden">` containing `<ClassSelector classes={classesWithResults} selectedClassId={selectedClassId} onchange={(id) => (selectedClassId = id)} />` followed by a single `<ResultsTable rows={rankings.get(selectedClassId!) ?? []} />`; grid branch `<div class="hidden gap-4 md:grid md:grid-cols-1 lg:grid-cols-2 xl:grid-cols-3 xl:gap-6">` (16px gap through the md/lg range, 24px only at xl/1280px+ per the UI-SPEC Spacing Scale table — do not use `lg:gap-6`, which would switch at 1024px instead) with one `<GlassCard class="p-4 md:p-6 xl:p-6">` per class in classesWithResults order, each containing an `<h2>` (Heading role, class name) and `<ResultsTable rows={rankings.get(cls.id!) ?? []} />`. Top of template: copy ScoreEntry.svelte's wrapper/heading/error-row shape (`strings.results.heading` as the h1, `errorFeedback` state var + red-text row — errorFeedback stays unused by this plan but is declared now since Plan 02 (reset) extends this same file and needs it).

    Modify src/App.svelte: change the import at line 11 from `import ResultsPlaceholder from './lib/views/ResultsPlaceholder.svelte';` to `import Results from './lib/views/Results.svelte';`, and change the views map entry at line 28 from `results: ResultsPlaceholder,` to `results: Results,`. Do not delete ResultsPlaceholder.svelte.

    Create src/lib/views/Results.test.ts (fake-indexeddb + @testing-library/svelte pattern, mirroring ScoreEntry.test.ts's beforeEach(resetDb)/render/seed-via-db.*.add structure) covering: empty state renders when no shooters exist; seeding 2 classes with shooters renders classesWithResults in alphabetical order regardless of insertion order; the phone wrapper div carries `md:hidden` and the grid wrapper div carries both `hidden` and `md:grid` in its class list (static class-presence check, matching the ScoreEntry.test.ts precedent for the Klasse-column responsive check); an incomplete shooter's row shows the in-progress marker end-to-end through the real view.

    Create e2e/results.spec.ts (new; mirrors e2e/nav.spec.ts's viewport-assertion shape and e2e/scoring.spec.ts's setup-flow-helper convention) with a `setUpTournamentWithResults(page)` helper that configures 1+ classes, 1+ shooting lines, a rounds config, registers 2+ shooters with different scores, and navigates to Ergebnisse. Add three viewport tests per the UI-SPEC's exact breakpoints: 375px width asserts the phone dropdown (`<select>` labeled strings.results.classDropdownLabel) is visible and no grid card is visible; 1024px width asserts the grid renders (2-column boundary, `lg:grid-cols-2` active) and no dropdown is visible; 1440px width asserts the grid renders at the 3-column breakpoint (`xl:grid-cols-3` active). Reset-flow tests are explicitly out of scope for this task (added in Plan 02) — do not add a reset test here.
  </action>
  <acceptance_criteria>
    - Results.svelte's phone-branch wrapper element's class attribute contains the substring "md:hidden"; the grid-branch wrapper element's class attribute contains both "hidden" and "md:grid"
    - Results.svelte's grid wrapper uses "xl:gap-6" (not "lg:gap-6") for the 24px-at-desktop gap per the UI-SPEC Spacing Scale table
    - `grep -n "results: Results" src/App.svelte` matches (views map updated) and `grep -n "ResultsPlaceholder" src/App.svelte` returns 0 matches (import removed)
    - Results.test.ts asserts alphabetical class order independent of db insertion order
    - `npx vitest run src/lib/views/Results.test.ts` exits 0
    - `npx playwright test e2e/results.spec.ts -g "phone|desktop"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/views/Results.test.ts && npx playwright test e2e/results.spec.ts -g "phone|desktop"</automated>
  </verify>
  <done>Navigating to "Ergebnisse" shows live, correctly-ranked per-class results; phone renders a single-class dropdown, tablet/desktop render a responsive 1/2/3-column grid; empty state shows when no shooters are registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| IndexedDB (Dexie) → Results view | Read-only data flow; the trainer's own device, no other party observes or supplies this data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Information Disclosure | Results.svelte (display of shooter names/scores) | accept | Single trusted trainer, single device, fully offline, no network transport — no other party can observe this app's IndexedDB-backed data (per CLAUDE.md's stated threat model; V2/V3/V4 ASVS categories confirmed inapplicable in 04-RESEARCH.md's Security Domain section) |
</threat_model>

<verification>
Run `npm run test -- ranking` after Task 1, `npm run test -- ResultsTable` after Task 2, `npm run test -- Results` and `npx playwright test e2e/results.spec.ts -g "phone|desktop"` after Task 3. Run `npm run test:all` once all three tasks are complete before considering this plan done.
</verification>

<success_criteria>
- `npx vitest run src/lib/utils/ranking.test.ts` — green, including the mandatory tie (1-2-2-4) and cross-round-sum fixtures
- `npx vitest run src/lib/components/ResultsTable.test.ts` — green
- `npx vitest run src/lib/views/Results.test.ts` — green
- `npx playwright test e2e/results.spec.ts -g "phone|desktop"` — green at 375px/1024px/1440px
- Manually navigating to "Ergebnisse" in a running dev build shows ranked, per-class results with no console errors
</success_criteria>

<output>
Create `.planning/phases/04-results/04-01-SUMMARY.md` when done
</output>
