---
phase: 04-results
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/views/Results.svelte
  - src/lib/views/Results.test.ts
  - e2e/results.spec.ts
autonomous: true
requirements: [RES-05]

must_haves:
  truths:
    - "Trainer can start a new tournament from the Results view itself via a clearly destructive-styled action, with no new nav section or settings area needed (D-08)"
    - "Reset requires an explicit, non-dismissible confirmation before anything is cleared"
    - "After confirming reset, all shooters and scores are gone but classes, shooting lines, rounds/passes, and presets remain untouched, per RES-05's locked wording (D-10)"
    - "If the clearing operation fails partway, the trainer sees an error message instead of a silently inconsistent database"
  artifacts:
    - path: "src/lib/views/Results.svelte"
      provides: "Reset button + ConfirmDialog wiring + atomic db.transaction reset handler"
  key_links:
    - from: "src/lib/views/Results.svelte"
      to: "src/lib/components/ConfirmDialog.svelte"
      via: "<ConfirmDialog destructive={true} onconfirm={handleResetConfirm} .../> reused verbatim (D-09)"
      pattern: "ConfirmDialog"
    - from: "src/lib/views/Results.svelte"
      to: "Dexie db.transaction"
      via: "db.transaction('rw', db.shooters, db.scores, async () => { await db.shooters.clear(); await db.scores.clear(); })"
      pattern: "db\\.transaction\\('rw'"
---

<objective>
Add the "Neues Turnier starten" (start new tournament / reset) action to the Results view: a destructive-styled button, `ConfirmDialog` reuse for confirmation, and an atomic Dexie transaction that clears only `shooters` and `scores`, leaving `classes`/`shootingLines`/`rounds`/`presets` untouched (RES-05, D-08/D-09/D-10).

Purpose: Lets the trainer start a same-shaped tournament again without reconfiguring Setup from scratch, while guaranteeing the destructive action is confirmed and cannot leave the database in a half-cleared state.
Output: `Results.svelte` extended with the reset flow; unit and e2e coverage proving the atomic clear-scope and the confirm/cancel behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-results/04-CONTEXT.md
@.planning/phases/04-results/04-UI-SPEC.md
</context>

<interfaces>
<!-- Results.svelte already exists after Plan 01 with the shape below — this plan extends
     it, it does not recreate it. -->

From src/lib/views/Results.svelte (created by Plan 01, current relevant state):
```typescript
const shootersQuery = liveQuery(() => db.shooters.toArray());
let shooters = $derived($shootersQuery ?? []);
const classesQuery = liveQuery(() => db.classes.toArray());
let classes = $derived($classesQuery ?? []);
const roundsQuery = liveQuery(() => db.rounds.get(1));
let roundsConfig = $derived($roundsQuery);
const scoresQuery = liveQuery(() => db.scores.toArray());
let allScores = $derived($scoresQuery ?? []);
let rankings = $derived(computeClassRankings(shooters, classes, allScores, roundsConfig));
let classesWithResults = $derived(/* alphabetical, non-empty classes only */);
let errorFeedback = $state('');
```

From src/lib/components/ConfirmDialog.svelte (existing, reuse verbatim):
```typescript
let { open, title, body, confirmLabel, cancelLabel, destructive = false, onconfirm, oncancel }: {
  open: boolean; title: string; body: string; confirmLabel: string; cancelLabel: string;
  destructive?: boolean; onconfirm: () => void; oncancel: () => void;
} = $props();
```

From src/lib/i18n/strings.de.ts's `results` section (added by Plan 01 Task 2, already present — do not re-add):
`resetButton`, `resetConfirmTitle`, `resetConfirmBody`, `resetConfirmYes`, `resetConfirmCancel`, `resetSuccess`, `resetError`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reset button, ConfirmDialog wiring, atomic transaction handler</name>
  <files>src/lib/views/Results.svelte</files>
  <read_first>
    src/lib/views/Results.svelte (current state after Plan 01 — read to see the exact existing errorFeedback declaration and template structure you are extending, not recreating)
    src/lib/components/ConfirmDialog.svelte (full file — props/usage contract)
    src/lib/views/ScoreEntry.svelte (lines 224-246 finalize confirm-then-write-then-catch shape, and lines 317-326 ConfirmDialog usage — the exact pattern to mirror, swapping the write for the transaction below)
    src/lib/i18n/strings.de.ts (results section — exact reset string keys already present from Plan 01)
  </read_first>
  <action>
    In Results.svelte, add `let resetDialogOpen = $state(false);` and `let resetSuccessMessage = $state('');`. Add a button below the phone/grid branches: label strings.results.resetButton, leading `RotateCcw` icon (20px, `@lucide/svelte`, import alongside existing icon imports if any), destructive styling matching ClassForm.svelte's delete-confirm button token (bg-red-600 hover:bg-red-700 text-white / dark:bg-red-400 dark:text-slate-900), `min-h-[44px]`, `w-full md:w-auto` (full-width phone, natural width desktop), `onclick={() => (resetDialogOpen = true)}`. Wire `<ConfirmDialog open={resetDialogOpen} title={strings.results.resetConfirmTitle} body={strings.results.resetConfirmBody} confirmLabel={strings.results.resetConfirmYes} cancelLabel={strings.results.resetConfirmCancel} destructive={true} onconfirm={handleResetConfirm} oncancel={() => (resetDialogOpen = false)} />` at the end of the template, matching ScoreEntry.svelte's ConfirmDialog placement pattern.

    Implement `async function handleResetConfirm() { resetDialogOpen = false; errorFeedback = ''; try { await db.transaction('rw', db.shooters, db.scores, async () => { await db.shooters.clear(); await db.scores.clear(); }); resetSuccessMessage = strings.results.resetSuccess; } catch (err) { errorFeedback = strings.results.resetError.replace('{error}', err instanceof Error ? err.message : String(err)); } }`. List ONLY `db.shooters, db.scores` in the transaction call (per Dexie's API, only tables read/written inside the callback need listing — `classes`/`shootingLines`/`rounds`/`presets` are never touched). Render `resetSuccessMessage` as a non-blocking success line (same visual weight as the existing errorFeedback red-text row, but neutral/teal, not destructive-red) directly above the reset button when non-empty; clear it (`resetSuccessMessage = ''`) the next time the reset button is clicked to open a fresh dialog.
  </action>
  <acceptance_criteria>
    - `grep -n "db.transaction('rw', db.shooters, db.scores" src/lib/views/Results.svelte` matches
    - The reset button's onclick opens resetDialogOpen; ConfirmDialog's destructive prop is literally `true`
    - `grep -c "db.classes.clear\|db.shootingLines.clear\|db.rounds.clear\|db.presets.clear" src/lib/views/Results.svelte` returns 0 (only shooters/scores are ever cleared here)
    - On handleResetConfirm success, resetSuccessMessage equals strings.results.resetSuccess; on a thrown error, errorFeedback equals strings.results.resetError with `{error}` replaced by the caught message
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/views/Results.test.ts -t "reset"</automated>
  </verify>
  <done>Clicking "Neues Turnier starten" opens the non-dismissible ConfirmDialog; confirming clears only shooters+scores atomically and shows a success message; a simulated write failure surfaces errorFeedback instead of failing silently.</done>
</task>

<task type="auto">
  <name>Task 2: Reset flow test coverage (unit + e2e)</name>
  <files>src/lib/views/Results.test.ts, e2e/results.spec.ts</files>
  <read_first>
    src/lib/views/Results.test.ts (as created by Plan 01 — append new describe block, do not restructure existing tests)
    e2e/scoring.spec.ts (full file — the finalize-then-reload pattern and setUpOneShooterOneArrowTournament helper shape to mirror for a reset-then-reload proof)
    e2e/results.spec.ts (as created by Plan 01 — append the reset test alongside the existing breakpoint tests)
  </read_first>
  <action>
    In Results.test.ts, add a `describe('reset (RES-05, D-10)', ...)` block: seed 1+ classes, 1+ shooters, 1+ scores via db.*.add/put; render(Results); click the reset button (screen.getByRole('button', { name: strings.results.resetButton })); assert the ConfirmDialog appears with strings.results.resetConfirmTitle; clicking cancel leaves db.shooters.count() and db.scores.count() unchanged; re-opening and clicking the confirm button (strings.results.resetConfirmYes) results in db.shooters.count() === 0 and db.scores.count() === 0 while db.classes.count() and db.rounds.get(1) remain exactly as seeded; assert the view re-renders to the empty state (strings.results.emptyHeading) and shows strings.results.resetSuccess.

    In e2e/results.spec.ts, add a `test.describe('reset flow (RES-05)', ...)` block that reuses/extends the setUpTournamentWithResults helper from Plan 01: after seeding a full tournament with results, click "Neues Turnier starten", confirm the dialog, assert the empty-state message appears, then `page.reload()` and assert (a) the Ergebnisse view still shows the empty state (proving the clear persisted, not just in-memory), and (b) navigating to Einrichtung shows the same classes/rounds/lines still configured (proving D-10's retention, not just the deletion).
  </action>
  <acceptance_criteria>
    - Results.test.ts's reset describe block asserts db.classes/db.rounds are unchanged after reset while db.shooters/db.scores are both empty
    - e2e/results.spec.ts's reset test asserts state survives a `page.reload()` (IndexedDB persistence, not component state)
    - `npx vitest run src/lib/views/Results.test.ts -t "reset"` exits 0
    - `npx playwright test e2e/results.spec.ts -g "reset"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/views/Results.test.ts -t "reset" && npx playwright test e2e/results.spec.ts -g "reset"</automated>
  </verify>
  <done>Both the unit and e2e reset-flow tests pass, proving atomic clear scope and reload-durable persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Trainer click → ConfirmDialog → Dexie write | A single trusted local operator initiates an irreversible, local data-clearing action; no remote/adversarial input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Repudiation / Tampering (self-inflicted, not adversarial) | Reset button + ConfirmDialog (Results.svelte) | mitigate | Reuse ConfirmDialog's existing non-dismissible, explicit-choice pattern (D-09) — no backdrop-click or Escape shortcut bypasses the confirm step, consistent with the app's established destructive-action mitigation (finalize, preset delete/overwrite) |
| T-4-03 | Tampering (data integrity, not adversarial) | handleResetConfirm's clear of db.shooters/db.scores | mitigate | Wrap both `.clear()` calls in `db.transaction('rw', db.shooters, db.scores, ...)` so a browser crash or storage-quota error mid-operation cannot leave scores referencing already-deleted shooters (or vice versa) |
</threat_model>

<verification>
Run `npm run test -- Results -t reset` after Task 1 and again after Task 2. Run `npx playwright test e2e/results.spec.ts -g reset` after Task 2. Run `npm run test:all` before considering this plan done, to confirm no regression to Plan 01's breakpoint tests.
</verification>

<success_criteria>
- `npx vitest run src/lib/views/Results.test.ts -t "reset"` — green
- `npx playwright test e2e/results.spec.ts -g "reset"` — green, including the reload-persistence assertion
- Manually: reset with an active tournament clears the roster/scores but Setup's classes/lines/rounds remain configured exactly as before
</success_criteria>

<output>
Create `.planning/phases/04-results/04-02-SUMMARY.md` when done
</output>
