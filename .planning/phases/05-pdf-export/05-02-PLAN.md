---
phase: 05-pdf-export
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - package.json
  - package-lock.json
  - src/lib/utils/pdfExport.ts
  - src/lib/utils/pdfExport.test.ts
  - src/lib/views/Results.svelte
  - src/lib/i18n/strings.de.ts
  - e2e/pdfExport.spec.ts
autonomous: true
requirements: [PDF-01, PDF-04, PDF-05, PDF-06, PDF-07]

must_haves:
  truths:
    - "Trainer can click 'PDF exportieren' on the Results view and a PDF file downloads to their device."
    - "The PDF contains one section per class (each starting on a new page), with a table of Rank, Name, Gesamt (Sum) columns only."
    - "Unchecking (default) the 'Unvollständige Ergebnisse einbeziehen' checkbox excludes shooters with incomplete score entry from the PDF; checking it includes them."
    - "The downloaded filename matches Ergebnisse_YYYY-MM-DD.pdf using today's ISO date."
    - "PDF generation makes zero network requests (works fully offline)."
  artifacts:
    - path: "src/lib/utils/pdfExport.ts"
      provides: "generateResultsPdf() pure function + resultsPdfFilename() helper, consuming computeClassRankings() output"
      exports: ["generateResultsPdf", "resultsPdfFilename"]
    - path: "src/lib/views/Results.svelte"
      provides: "PDF export button + include-incomplete checkbox, wired to db.settings and computeClassRankings"
      contains: "generateResultsPdf"
  key_links:
    - from: "src/lib/views/Results.svelte"
      to: "src/lib/utils/pdfExport.ts"
      via: "generateResultsPdf(rankings, classes, settings, includeIncomplete)"
      pattern: "generateResultsPdf\\("
    - from: "src/lib/utils/pdfExport.ts"
      to: "jspdf-autotable"
      via: "autoTable(doc, {...}) per-class table rendering"
      pattern: "autoTable\\("
    - from: "src/lib/views/Results.svelte"
      to: "src/lib/db/schema.ts (db.settings)"
      via: "liveQuery(() => db.settings.get(1)) read for title/logos at export time"
      pattern: "db\\.settings\\.get"
---

<objective>
Deliver the complete "trainer exports ranked results as a PDF" vertical slice: install jsPDF + jspdf-autotable, implement a pure `generateResultsPdf()` function consuming the existing `computeClassRankings()` output plus the Plan 01 `settings` table, and wire an export button + include-incomplete checkbox into the Results view that triggers a real browser download.

Purpose: This is the phase's core value delivery — completes PDF-01/04/05/06/07 and makes the Phase 5 goal ("export tournament results as a PDF") user-reachable end-to-end.

Output: `src/lib/utils/pdfExport.ts` (tested pure function), updated `Results.svelte` with working export button/checkbox, and passing unit + E2E tests covering column layout, incomplete-shooter filtering, pagination, filename format, and offline operation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pdf-export/05-CONTEXT.md
@.planning/phases/05-pdf-export/05-RESEARCH.md
@.planning/phases/05-pdf-export/05-UI-SPEC.md
@.planning/phases/05-pdf-export/05-01-SUMMARY.md

<interfaces>
<!-- RankedRow shape this plan's pdfExport.ts consumes directly (src/lib/utils/ranking.ts,
     unchanged by this phase) -->
```typescript
export interface RankedRow {
  shooterId: number;
  name: string;
  line: number | null;
  sum: number;
  rank: number;
  isComplete: boolean;
}
export function computeClassRankings(
  shooters: ShooterRecord[], classes: ClassRecord[], scores: ScoreRecord[], roundsConfig: RoundConfig | undefined
): Map<number, RankedRow[]>;
```

<!-- SettingsRecord shape from Plan 01 (src/lib/db/schema.ts) -->
```typescript
export interface SettingsRecord {
  id: 1;
  title?: string;
  logoLeftBlob?: Blob;
  logoRightBlob?: Blob;
}
```

<!-- Results.svelte's existing liveQuery + $derived pattern this plan extends (read the
     full current file before editing — this plan adds to the same script block that
     already defines `shooters`, `classes`, `roundsConfig`, `allScores`, `rankings`,
     `classesWithResults`, and the reset-dialog state). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install jsPDF/jspdf-autotable + pure PDF generation function</name>
  <files>package.json, src/lib/utils/pdfExport.ts, src/lib/utils/pdfExport.test.ts</files>
  <read_first>src/lib/utils/ranking.ts, src/lib/db/schema.ts, package.json</read_first>
  <behavior>
    - Test: `generateResultsPdf()` given a `Map` with 2 classes (each with 2+ ranked rows) produces a `Blob` with `type === 'application/pdf'` and non-zero `size`.
    - Test: given `includeIncomplete = false` (default), rows where `isComplete === false` are excluded from the table data passed to `autoTable`; given `includeIncomplete = true`, all rows are included. Assert by spying/inspecting the `body` array built before calling `autoTable` (extract table-building into an exported, independently testable helper e.g. `buildClassTableRows(rows, includeIncomplete)` returning `string[][]` of exactly `[rank, name, sum]` per row — no `line` or class fields).
    - Test: `buildClassTableRows` never emits more than 3 columns per row (Rank, Name, Sum only — PDF-04).
    - Test: `resultsPdfFilename(date: Date)` returns `Ergebnisse_YYYY-MM-DD.pdf` for a given fixed `Date` (e.g. `new Date('2026-07-06T10:00:00Z')` -> `'Ergebnisse_2026-07-06.pdf'`).
    - Test: `generateResultsPdf()` with a `classifications` Map containing 3 classes calls `doc.addPage()` exactly 2 times (once before each class after the first) — assert via a jsPDF instance page count (`doc.getNumberOfPages()` equals the number of classes when each class only needs one page).
  </behavior>
  <action>
    Run `npm install jspdf@4.2.1 jspdf-autotable@5.0.8` (adds to `dependencies`, not `devDependencies` — these run in the production browser bundle, per RESEARCH.md Package Legitimacy Audit; both packages are pre-approved, no legitimacy checkpoint required).

    Create `src/lib/utils/pdfExport.ts` exporting:
    - `buildClassTableRows(rows: RankedRow[], includeIncomplete: boolean): string[][]` — filters by `isComplete` when `includeIncomplete` is false (explicit positive-boolean naming per RESEARCH.md Pitfall 6, no inverted logic), maps to `[row.rank.toString(), row.name, row.sum.toString()]`.
    - `resultsPdfFilename(date: Date = new Date()): string` — `` `Ergebnisse_${date.toISOString().split('T')[0]}.pdf` ``.
    - `generateResultsPdf(classifications: Map<number, RankedRow[]>, classes: ClassRecord[], settings: { title?: string; logoLeftBlob?: Blob; logoRightBlob?: Blob }, includeIncomplete: boolean): Promise<Blob>` — per RESEARCH.md Pattern 1/Code Example: `new jsPDF({ orientation: 'portrait', unit: 'mm', format: 'a4' })`; import `autoTable` as the functional default export (`import autoTable from 'jspdf-autotable'`) and call `autoTable(doc, { head: [['Rang', 'Name', 'Gesamt']], body: buildClassTableRows(rows, includeIncomplete), startY, theme: 'striped', styles: { fontSize: 10 }, headStyles: { fillColor: [100,100,100], textColor: 255, fontStyle: 'bold' } })` per 05-UI-SPEC.md's PDF Output Styling section (A4 portrait, 20mm margins, 14pt bold class heading, 10pt title line, 25mm x 20mm logos at 20mm from left/right edge). Sort classes alphabetically via `classes.filter(c => c.id !== undefined && classifications.has(c.id)).sort((a,b) => a.name.localeCompare(b.name))` (same ordering as Results.svelte's existing `classesWithResults`). Call `doc.addPage()` before every class except the first (D-01). Convert `logoLeftBlob`/`logoRightBlob` to data URIs via `FileReader.readAsDataURL` before `doc.addImage()`, skip image rendering entirely if a Blob is absent. Return `doc.output('blob')`.
  </action>
  <verify>
    <automated>npm run test -- src/lib/utils/pdfExport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/lib/utils/pdfExport.test.ts` exits 0 with all 5+ described test cases passing.
    - `grep -c "jspdf" package.json` and `grep -c "jspdf-autotable" package.json` are each at least 1 under `dependencies` (not `devDependencies`) — verify via `node -e "console.log(require('./package.json').dependencies.jspdf, require('./package.json').dependencies['jspdf-autotable'])"` printing both version strings.
    - `buildClassTableRows` output rows never contain more than 3 elements (enforced by test assertion).
    - `resultsPdfFilename(new Date('2026-07-06T10:00:00Z'))` returns exactly `'Ergebnisse_2026-07-06.pdf'`.
  </acceptance_criteria>
  <done>jsPDF + jspdf-autotable are installed as production dependencies; `generateResultsPdf()`, `buildClassTableRows()`, and `resultsPdfFilename()` exist, are independently unit-tested, and satisfy PDF-01/04/05/07 in isolation from any UI.</done>
</task>

<task type="auto">
  <name>Task 2: Wire export button + checkbox into Results view</name>
  <files>src/lib/views/Results.svelte, src/lib/i18n/strings.de.ts, e2e/pdfExport.spec.ts</files>
  <read_first>src/lib/views/Results.svelte, src/lib/i18n/strings.de.ts, src/lib/utils/pdfExport.ts</read_first>
  <action>
    Append a `resultsPdf` (or extend the existing `results`) section to `strings.de.ts` with the exact copy from 05-UI-SPEC.md's Copywriting Contract: `exportButton: 'PDF exportieren'`, `includeIncompleteLabel: 'Unvollständige Ergebnisse einbeziehen'`, `includeIncompleteHelper: 'Schützen mit unvollständigen Ergebnissen in den PDF aufnehmen'`, `exportError: 'Speichern fehlgeschlagen'`.

    In `Results.svelte`, add: `const settingsQuery = liveQuery(() => db.settings.get(1)); let settings = $derived($settingsQuery ?? { id: 1 as const });`, `let includeIncomplete = $state(false);` (default unchecked per D-09), and an async `handleExport()` that calls `generateResultsPdf(rankings, classesWithResults, settings, includeIncomplete)`, then triggers download via `const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = resultsPdfFilename(); a.click(); URL.revokeObjectURL(url);`, wrapped in try/catch setting `errorFeedback = strings.resultsPdf.exportError` on failure (reuse the existing `errorFeedback` state variable already declared in this file). Render the checkbox (`<input type="checkbox" bind:checked={includeIncomplete}>` with label `strings.resultsPdf.includeIncompleteLabel` and helper text `strings.resultsPdf.includeIncompleteHelper`) and the "PDF exportieren" button (`min-h-[44px]`, `bg-teal-500 dark:bg-teal-400`, disabled when `classesWithResults.length === 0`) inside a new GlassCard section placed between the results grid and the existing "Neues Turnier starten" reset button, per 05-UI-SPEC.md's Results View layout spec (lg/24px gap above).

    Create `e2e/pdfExport.spec.ts` following `e2e/presetExportImport.spec.ts`'s pattern (production `vite preview` build, `page.waitForEvent('download')`): register at least one class + shooter + complete scores via the UI (or seed via a test setup helper matching existing e2e conventions), navigate to Ergebnisse, click "PDF exportieren", assert `download.suggestedFilename()` matches `/^Ergebnisse_\d{4}-\d{2}-\d{2}\.pdf$/`. Add a second test that sets `await page.context().setOffline(true)` before clicking export and asserts the download still succeeds (PDF-06, zero network dependency).
  </action>
  <verify>
    <automated>npm run test && npx playwright test e2e/pdfExport.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "generateResultsPdf" src/lib/views/Results.svelte` is at least 1.
    - `grep -c "includeIncomplete" src/lib/views/Results.svelte` is at least 2 (state declaration + checkbox binding).
    - `e2e/pdfExport.spec.ts` passes both the filename-format test and the offline-mode test.
    - Manual check: with zero shooters registered, the "PDF exportieren" button is disabled (mirrors existing `finalizeButton` disabled-state pattern from ScoreEntry.svelte).
  </acceptance_criteria>
  <done>Trainer can toggle the include-incomplete checkbox, click "PDF exportieren" on the Results view, and receive a correctly-named PDF download containing the expected per-class sections — verified automated end-to-end, including with the browser offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Settings title string -> PDF text | Free-text title (Plan 01, user-controlled) crosses into `doc.text()` rendering inside the generated PDF. |
| Browser -> filesystem | Generated Blob crosses into a real file download via anchor + `URL.createObjectURL`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering/Injection | `pdfExport.ts` title rendering | accept | `doc.text()` is a PDF primitive, not an HTML/JS sink — no script execution risk from arbitrary title strings; jsPDF does not interpret markup. |
| T-05-05 | Denial of Service | `generateResultsPdf()` with very large ranking sets | accept | Tournament sizes are bounded by manual registration (single trainer, single device, realistic archery-club scale — tens, not thousands, of shooters); jspdf-autotable's pagination handles arbitrary row counts without custom code. |
| T-05-SC | Tampering | npm install of `jspdf`/`jspdf-autotable` | mitigate | Both packages pre-audited in 05-RESEARCH.md's Package Legitimacy Audit table with `[OK]` slopcheck verdicts and no `[ASSUMED]`/`[SUS]` flags — no blocking human-verify checkpoint required per that audit. |
</threat_model>

<verification>
Run `npm run test` (full unit suite including pdfExport.test.ts) and `npm run test:e2e -- e2e/pdfExport.spec.ts` (Playwright, production build, including the offline-mode assertion). Manually open a generated PDF in a real viewer (Preview/Adobe/browser PDF viewer) and confirm: correct section headings, Rank/Name/Gesamt columns only, page break before each class, logos render if configured in Plan 01's Settings form.
</verification>

<success_criteria>
- jsPDF + jspdf-autotable installed as production dependencies.
- `generateResultsPdf()` produces a correctly structured, paginated multi-class PDF from real `computeClassRankings()` output.
- Results view export button + checkbox are fully wired and functional, including the offline case.
- All unit and E2E tests for this plan pass.
</success_criteria>

<output>
Create `.planning/phases/05-pdf-export/05-02-SUMMARY.md` when done
</output>
